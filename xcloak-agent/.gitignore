# Secrets
.env
agent.token

# Build artifacts
tmp/
dist/
*.exe
*.out

# eBPF compiled objects (*.o is excluded, but generated bpf2go objects must be
# committed — they are embedded at compile time via //go:embed and cannot be
# regenerated in CI without clang)
*.o
!agent/ebpf/*.o
