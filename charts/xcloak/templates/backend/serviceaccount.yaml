{{- if .Values.backend.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.backend.serviceAccount.name | default (include "xcloak.backend.fullname" .) }}
  labels:
    {{- include "xcloak.backend.labels" . | nindent 4 }}
{{- end }}
