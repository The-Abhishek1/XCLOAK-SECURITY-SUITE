# Page-by-Page Testing Progress

Tracks the live-stack Playwright testing phase (started 2026-07-27): one sidebar
page at a time — audit frontend + backend for hardcoded/fabricated/dead data
and broken routes, fix real bugs found, write `tests/e2e/<page>.spec.ts`
against the real backend + seeded DB, verify `go build`/`go vet`/`go test` +
`tsc`/`lint`, run the full accumulated suite, update this file and the
project memory (`project_page_testing_phase.md`).

**How to use this file**: read it at the start of a session to see what's
next; update the relevant checklist line(s) at the end of every page (move
from ⬜ to ✅ with a one-line summary + spec file name). Full detail for each
page lives in memory (`project_page_testing_phase.md`), not here — this file
is just the index/checklist.

Full suite status as of last page (DFIR): **run in full this time** — 215
passed, 15 failed, 4 skipped (234 total, ~8 minutes). All 15 failures are in
already-completed pages unrelated to DFIR's own changes (`agents`,
`clusters`, `elastic-query`, `hunt-workbench` AI-Explain, `incidents` ×6,
`network-map`, `risk-posture`, `threat-detection` ×2, `threat-intel` AI) —
`dfir.spec.ts` itself passed 8/8 cleanly within this same run. 3 of the 15
are the already-documented CPU-bound local-LLM contention class
(`elastic-query`, `hunt-workbench`, `threat-intel` — see below).
**`incidents.spec.ts` failing 6/6 in one run is new and worth flagging**:
this exact page had a "3-for-3 identical failure" streak flagged after the
YARA Rules pass, then broke the streak (passed cleanly) during the JA3
Fingerprints pass — now it's failed completely again, harder than before.
Nothing in DFIR's or Threat Hunt's changes touches Incidents' code path,
and the pattern (whole-page failure under a big parallel run, not a
specific assertion) matches the same load-contention signature as every
prior incident this phase rather than a standing regression — but if it
fails a 4th/5th time on an unrelated future page's run, it's worth an
actual dedicated look instead of continuing to wave it off as load.
**Growing self-inflicted flake class — escalating, worth addressing soon**:
this phase now has **5** CPU-bound local-LLM tests spread across 4 spec
files (`elastic-query.spec.ts` ×2, `log-sources.spec.ts` ×1,
`clusters.spec.ts` ×1, `threat-intel.spec.ts` ×1 — each 20-90s+ on this
machine's GPU-less Ollama) that can contend with *each other* under
full-suite parallelism; this run's failures include `clusters.spec.ts`'s
AI test timing out at a full 120s budget for the first time (previously
always passed standalone) purely from this contention. Every one of the 5
has independently passed cleanly in isolation/small batches, so a solo
failure among them under a big parallel run is still this known class, not
a regression — but the count keeps growing as more AI-backed pages get
tested, and it may be worth proposing a real fix (e.g. tagging these tests
to run serially relative to each other, or in a separate low-concurrency
project) before it gets worse. A mid-session memory-pressure incident
(swap in use, load avg 7-11) was resolved by restarting the long-running
`next dev` process — same "restart before assuming a real regression"
lesson as the Attack Paths incident earlier this phase.
**Infra note**: this dev environment had no working LLM at all this session
(`.env` said `LLM_PROVIDER=ollama` but Ollama was never installed) — every
AI-backed endpoint app-wide was silently returning "AI service unavailable".
Installed Ollama + pulled `qwen2.5:3b` (matching `.env`'s `OLLAMA_MODEL`) to
unblock testing; it's slow (30-90s/call, CPU-only) but functional. If AI
features on a future page seem broken, check `ollama list` /
`curl localhost:11434/api/tags` first, not the page's code.

---

## ✅ Completed (50)

1. **Dashboard** (`/dashboard`) — `dashboard.spec.ts`. Fabricated compliance %, broken SQL (nonexistent `compliance_reports.framework`/`.details` columns).
2. **Agents** (`/agents`) — `agents.spec.ts`. Dead `is_isolated`/`tamper_protection` fields fixed; removed a duplicate in-page "Enroll Agent" onboarding modal in favor of the more complete `/agents/onwards` wizard. **Agent Detail (`/agents/:id`) has 2 known deferred bugs** — start there when it comes up (see memory).
3. **Alerts** (`/alerts`) — `alerts.spec.ts`. Removed a huge fake-investigation drawer (fake scores/identities/fake action buttons), fixed a stale-closure bug + a race condition. **Revisited during Correlation work**: found/fixed a severe bug where `GetAlertsPaginated` (and 4 sibling queries) silently dropped real alerts with NULL `agent_id`/MITRE fields from results while still counting them in `total`.
4. **Incidents** (`/incidents`) — `incidents.spec.ts`. Fake MTTC, a broken MTTR query (wrong column), fake SLA sub-milestones.
5. **UEBA** (`/ueba`) — `ueba.spec.ts`. 7 of 8 response actions were fully fake (canned success, no real effect); 5 components of dead detection UI removed. Session-revocation isn't actually enforced anywhere (flagged for Settings).
6. **Insider Threat** (`/insider-threat`) — `insider-threat.spec.ts`. Same fake-action bug as UEBA (copy-pasted code) + a demo-seeder contributor-key mismatch.
7. **NBA / Net Behavior** (`/nba`) — `nba.spec.ts`. 3/9 response actions rewired to real dispatch; fake domain-IOC cross-join in threat-intel; seed `anomaly_type` mismatch hid real data; a cross-page `firewall_rules` COALESCE bug; a window-selector race condition. **Revisited during Threat Intel work**: a repo-wide grep for the recurring `ioc_blocks`-doesn't-exist bug turned up a live, still-broken instance in `GetNBAAnalytics` — `blocked_ips` always silently returned 0; fixed to count real enabled IP-type IOCs instead (now correctly returns 11).
8. **DPI / Deep Inspection** (`/dpi`) — `dpi.spec.ts`. Built out a full 14-tab dashboard (12 real backend endpoints had zero UI). Found wrong-schema bugs in `iocs`/`ja3_fingerprints`/`incidents`/`alerts` that also affected NBA's already-"fixed" response actions — verify persistence with a live DB query, not just by reading the SQL.
9. **Behavioral / Threat Detection** (`/threat-detection`) — `threat-detection.spec.ts`. Nonexistent `ioc_blocks` table (again), a literal hardcoded `"triggered": 0`, `yara_matches` wrong-column bug in 3 places, IOC tab's `value`/`indicator` field mismatch, a misleading-affordance Library tab. Found+fixed a non-idempotent `cmd/seed/rules` duplicate-rule bug.
10. **Correlation** (`/correlation`) — `correlation.spec.ts`. Cleanest page yet. Simulation tab was permanently inert (wrong columns + zero seed data); "IOC/YARA match always creates an incident" was only true for 2 of 6 real rule-name variants. **Also fixed cross-cutting infra**: replaced per-file logins with a single shared `global-setup.ts`; found/fixed the Alerts pagination bug (see #3); found (but did not fully fix — flagged for later) an Alert Clusters `source_ip`-doesn't-exist bug in `cluster_enterprise.go`.
11. **Network Map** (`/network-map`) — `network-map.spec.ts`. A genuinely sophisticated real topology/enrichment engine underneath (real node/edge construction, real hostname/OS-based type inference, real CIDR cloud-IP detection, real IOC + ip-api.com threat intel). Bugs found: `alertCountsByAgent` queried nonexistent `alerts.acknowledged` instead of `status='open'`, silently zeroing every node's alert count and the "alerting_nodes" summary; the color/type Legend statically listed all 21 `INFRA_FILL` entries but the backend can only ever produce 9, so it implied detection of 12 fictional infra types (Kubernetes, Hypervisor, OT/ICS, etc.) — trimmed to the real 7 non-agent/non-external_ip types; the node detail drawer's Actions tab was the same 100%-fake-button pattern as UEBA/Insider Threat (11 buttons, zero API calls) — rewired the real subset (isolate_host/vulnerability_scan/collect_processes/collect_auth_logs task dispatch, plus block_ip/create_incident/add-to-IOC via the shared response-action dispatcher) and removed buttons with no real backend capability anywhere (Ping, Port Scan, Restart Agent — executor has no case for it, Whois Lookup); also removed the Actions tab for infra-type nodes (no agent_id to act on) and external_ip's dead "Traffic" tab (never showed distinct content).
12. **Attack Paths** (`/attack-path`) — `attack-path.spec.ts`. Another genuinely strong real engine (real Dijkstra shortest-compromise-path, real BFS blast radius, real MITRE technique mapping, real chokepoint detection). Bugs found: PATH_TYPE_TABS had 6 filter tabs (Cloud/Container/Identity/VPN/Hybrid/SaaS) that could never match a real path (backend's PathType is hard-limited to lateral/priv_esc) — always rendered "No paths match this filter"; the kill-chain model selector's Lockheed/Diamond options never highlighted anything (phase IDs never matched real backend values) and even the MITRE list itself had 3 dead phases + was missing the real "exploitation" phase — rebuilt to the 7 real phases, removed Lockheed/Diamond; the node detail drawer's Actions tab was the same 100%-fake-button pattern (10 buttons) — rewired 3 real ones (isolate_host/vulnerability_scan task dispatch, create_incident via the shared dispatcher), removed the rest, and hid Actions entirely for the internet node; removed a fully-fabricated "Est. impact ₹X.XM" business-impact figure with zero real backing data. **Also fixed a masked backend bug**: `CalculateRiskScore` had no upper-bound clamp (routinely exceeded 100 given this tenant's alert volume) — invisible until now because `asset_risk_scores` had zero rows for the demo tenant (no seeder ever populated it); added `seedRiskScores` to `cmd/seed/demo/main.go` and fixed the clamp, giving this page's central per-node metric real data for the first time.
13. **Risk Posture** (`/risk-posture`) — `risk-posture.spec.ts`. Mostly careful, honest backend code (explicit "don't fabricate a mapping that doesn't exist" comments throughout) undermined by one real backend bug and three frontend fabrications. **Backend**: `RefreshVulnPriorityScores` joined a nonexistent `agents.risk_score` column (real scores live in `asset_risk_scores`) — broke on every call (startup + every 6h tick, every tenant) since this scheduler's introduction, silently zeroing `priority_score` everywhere and permanently capping Risk Posture's VulnScore at 0/30. Fixed the join + added error logging. **Frontend**: the Compliance card invented its own CIS/NIST/ISO/SOC2 percentages (arbitrary decimal scaling + hardcoded fake control counts) instead of using the real `/api/compliance/scores/latest` endpoint the Dashboard already correctly uses — wired it in; a fabricated "Estimated Financial Exposure ₹X Cr/L" figure (same arbitrary-multiplier pattern as Attack Paths' "Est. impact") — removed; the Remediation Queue's status badge unconditionally claimed 'in_progress'/'blocked' for specific categories regardless of real state — normalized to the only honest value, 'open'; AI Recommendation buttons had no onClick/href at all (dead-end affordance) — wired 4 of 5 to real destination pages.
14. **Deploy Agent** (`/agents/onwards`) — `agents-onwards.spec.ts`. A small, mostly well-built onboarding wizard — backend token generation/redemption (`api/integrations.go` + `api/agents.go`'s `RegisterAgent`) is real and correctly built: single-use via an atomic `UPDATE ... WHERE used=false ... RETURNING`, tenant-scoped, expires via a genuine 24h DB column default. One real bug: Step 4's `checkForAgent` had `if (recent || agents.length > 0) setFound(true)` — the `|| agents.length > 0` fallback made the "did MY new agent register" check meaningless for any tenant with pre-existing agents (essentially all of them, including this demo tenant's 4 seeded agents), so clicking "Check Now" unconditionally showed "Agent detected!" on the very first click regardless of whether the just-onboarded agent ever actually ran. Fixed by removing the fallback.
15. **Timeline** (`/timeline`) — `timeline.spec.ts`. Arrived mostly already fixed from an earlier untracked pass (`timeline_service.go`'s SQL already COALESCEs every nullable column; the right-click "Filter by Host" menu item, previously a literal no-op, was already wired up — both verified live, not assumed from comments). Two new bugs found: selecting a specific agent routed through a separate `/agents/:id/timeline` endpoint that ignores severity/category/search/time-range filters entirely, silently dropping them while the toolbar kept showing them as active — fixed by routing agent selection through the general `/api/timeline` endpoint's existing `agent_id` param instead; and the evidence panel's Bookmark/Copy-JSON buttons plus the toolbar's Bookmarks-filter toggle were icon-only with the app's null-stubbed icon library, making two of them literally zero-size and unclickable (the same "icon-stubs null fallout" bug class fixed on 16 other files in an earlier pass, missed here) — fixed with the established symbol/text vocabulary.
16. **Live Logs** (`/live-logs`) — `live-logs.spec.ts`. Core streaming is real and solid: `/api/ws/ticket` + `LiveLogsWS` genuinely stream real `endpoint_logs` rows over a real WebSocket, and AI Explain/Summarize genuinely call the LLM. The `'demo-ws-ticket-noop'` fallback is not a bug — it's the frontend's separate, documented no-backend "static-demo" mode, confirmed the real backend never returns that value. Everything *around* that real core was dead analyst-workflow chrome: right-click "Create IOC"/"Create Alert Rule" and the 4-button "Analyst Actions" grid were pure no-ops with no onClick at all — wired "Create IOC" to the real `iocsAPI.create` (verified via curl: persists correctly), gave "Create Alert Rule" an honest coming-soon toast, and pointed the 4 grid buttons at their real destination pages (/sigma-rules, /hunt-workbench, /cases, /dfir). The Analyst Note textarea silently discarded whatever was typed the instant a different log was selected (state was keyed only to the open log, never lifted); made it session-scoped per log id. The Tags input+button had no onClick and no path back to parent state at all — broken end-to-end with no backend column to save to either — removed. Three static "AI" suggestion rows had no onClick; two now call the real Explain/Summarize handlers just above them, the third links to Sigma Rules. The "Columns" button in Table view was a literal `onClick={() => {}}` despite the column-visibility state already being fully wired through `TableView` — added the missing checkbox popover. **Deferred, cross-cutting finding, not fixed**: every seeded `endpoint_logs` row in this tenant has `parsed_fields = '{}'` — meaning the field-based search mode, field filters, FieldChips, and several real backend detectors that key off `parsed_fields->>'event_id'` etc. (`persistence_detector.go`, `behavioral_baseline_service.go`, and more) have never been exercised against real seed data in this demo tenant. This is a demo-seeder completeness gap spanning multiple detection services, not specific to this page — flagged for its own dedicated pass rather than attempted here.
17. **Log Search** (`/log-search`) — `log-search.spec.ts`. The AI-query bar, Detection Builder (real LLM calls), and the hourly-volume chart derivation were already real/fixed from an earlier untracked pass. Found a real, previously-latent **backend timezone bug** in `expandTimeRange` (`api/log_search.go`): the dev host's Go process runs in `Asia/Kolkata` (UTC+5:30), and `endpoint_logs.collected_at` is `timestamp without time zone` — Postgres silently drops the offset (rather than converting) when a tz-aware value is cast into a tz-less column, so every relative window (`5m/15m/1h/6h/24h/7d/30d`) was being filtered from ~5.5h in the future back to "now", systematically excluding real recent rows. `range=24h` returned 0 results even with real matching data sitting in Postgres — only caught because a fresh Playwright search returned empty and a raw `psql` count of the same window disagreed. Fixed by forcing `time.Now().UTC()` before computing any relative bound. Also fixed the actual page-scoped bugs: `scheduled_log_searches` had no migration ever, so every "Schedule" click silently saved nothing (added migration 000072 + fixed `CreateScheduledSearch`'s response to report the real `enabled: true`); the right-click menu's "Create Alert Rule"/"Create Sigma Rule" were no-ops, now open the existing real Detection Builder modal, and "Add to Case"/"Hunt Similar" now open their real destination pages; the Scheduled Searches list (both sidebar renders) had `DELETE /api/logs/scheduled/:id` fully wired in `logSearchAPI` but zero UI ever called it — added a delete button matching the existing Saved Searches pattern. **Revisited during ES Query work**: 3 of these newly-added delete/star buttons (plus 1 pre-existing Saved-Search delete) turned out to be genuinely zero-size/unclickable due to the icon-stubs-null-fallout bug class — fixed with the established ×/★/☆/🗑️ vocabulary (see #18).
18. **ES Query** (`/elastic-query`) — `elastic-query.spec.ts`. Backend is honest and well-built: every real-execution endpoint (`/elastic/query`,`/indices`,`/mappings`) correctly 503s "Elasticsearch not configured" instead of faking success (this dev stack has no ES container anywhere — confirmed across all `docker-compose*.yml`), already covered by existing Go integration tests. The AI-backed endpoints (`/elastic/explain`, `/ai/es-query`) don't need ES at all. Found via the same "grep for icon-only buttons" cheap check flagged at the end of the Live Logs section: **8 buttons were genuinely zero-size/unclickable** (icon-stubs.ts renders every icon as literal `null`, and these had no padding) — tab-close ×, saved-query star/delete, and all 5 modal close buttons (AI Assist/Agg Builder/Save/REST API/Explain) — fixed with the established ×/★/☆/🗑️ symbol vocabulary + `g-btn g-btn-ghost` padding; 2 more (hit-detail Copy, REST-API-code Copy) had padding so were clickable but showed a blank pill — added Copy/✓ text. **This same bug class was also found lurking on the already-completed Log Search page** (see #17) while auditing this one, confirming the "cheap to grep for" note actually pays off across pages, not just within one. **Infra incident, not a page bug**: discovered `.env`'s `LLM_PROVIDER=ollama` had no matching installed/running Ollama anywhere on this machine — every AI feature on every page has been silently returning "AI service unavailable" this whole session (confirmed via curl against Log Search's Detection Builder too). User chose to install Ollama; pulled `qwen2.5:3b` to match `.env`. Now genuinely functional but slow (30-90s/call, CPU-only, no GPU) — caused 2 new tests (this page's own AI-Assist verification, and Log Search's scheduled-search round trip) to time out under full-suite parallel load from CPU starvation; fixed by dropping a redundant duplicate LLM call and bumping timeouts, confirmed stable in a smaller concurrent rerun.

19. **Log Sources** (`/log-sources`) — `log-sources.spec.ts`. Uses real `lucide-react` icons (not the null-stubbed `lib/icon-stubs.ts`), so the icon-invisibility bug class doesn't apply here — but this page had the densest cluster of hardcoded-fake-status fields found yet, all in `api/log_sources_enterprise.go`: Health tab's "Parsing"/"Auth" rows were hardcoded `"ok"` with zero real check; Stats tab's "Compression: 3:1" and storage estimate's ÷3 divisor were fabricated — migration 000067 explicitly documents `endpoint_logs` as exempt from TimescaleDB compression (a natively-partitioned table TimescaleDB can't wrap), so no compression of any ratio applies; "Dropped Logs"/"Queue Length" claimed a queue/drop mechanism that doesn't exist anywhere in this fully-synchronous ingest path; Test Connection's "Auth"/"Parser"/"Permissions" were hardcoded `"ok"` and "Latency" a fixed literal `12`ms — replaced with a real DB-round-trip timing. The Parser tab's "ECS Field Mapping" table claimed dotted ECS paths (`source.ip`, `host.name`, `winlog.event_id`) that nothing in this codebase ever produces, in Postgres or the optional Elasticsearch mirror (confirmed by reading `elasticsearch_service.go` — it indexes `parsed_fields` verbatim, no ECS renaming anywhere) — removed entirely rather than half-fixed; several of its per-field claims were wrong even as aspirational docs (CEF's real field is `device_product`, not "product"). Found and fixed a real, separate bug in the same function: `parserForSource`'s format matching was case-sensitive against free-text `format` data that's never case-normalized at creation time — seeded sources declared as "CEF"/"JSON"/"Windows Event" (as opposed to lowercase) silently fell through to "Auto-Detect Parser" instead of their real parser; verified live via curl against 3 seeded sources before and after the fix. The "Alerts" tab was fully fake: 6 toggle-switches styled as clickable (cursor-pointer, sliding thumb) with hardcoded `active` booleans and **no onClick handler at all** — no per-source alert-rule API exists anywhere; removed the tab entirely. The "Receiver Endpoints" card's Syslog code block literally instructed users to point real infrastructure at a nonexistent `<host>:6514 TLS` listener (`services/syslog_receiver.go` only binds plain UDP/TCP on :514) — the same fake claim also appeared in the Config tab and the decorative Pipeline tab's Storage-stage description; all three fixed. **Revisited during ES Query's icon-audit trigger, confirming that check pays off cross-page again.**
20. **Alert Clusters** (`/clusters`) — `clusters.spec.ts`. Picked up the `source_ip`-doesn't-exist bug flagged (but not fixed) during the Correlation page's pass. The clustering engine (`services/alert_clustering_service.go`) and most of `api/cluster_enterprise.go` are real and well-built (already correctly fixed in an earlier pass — auto-promotion records a real `incident_events` row instead of a no-op `alerts.incident_id` UPDATE against a column that doesn't exist). Found `alerts` genuinely has no `source_ip` column at all (confirmed via `\d alerts`) — `GetClusterDetail`'s "Distinct source IPs" panel and `GetClusterGraph`'s IP nodes both still referenced it directly, so both queries always failed outright (error discarded) and were permanently empty; fixed by joining `agents` (matching the adjacent "Distinct hosts" query's existing pattern) and using the originating agent's real `ip_address` instead — verified live via curl. `GetClusterTimeline` had already been fixed for the identical issue in an earlier pass per its own comment, but the fix never actually populated a `source_ip` field, so the frontend's already-correct `e.source_ip ? ... : ''` conditional was still always blank — wired to the same real agent IP. **Also found, via manual review beyond the known bug**: the AI panel's cluster selector explicitly promises "uses most recent if blank", but `PostClusterAI` never implemented that fallback — a blank selection (`cluster_id: 0`) sent zero cluster context to the LLM, which hallucinated a plausible-looking but entirely fictional analysis; verified live via curl before the fix (invented systems "DBS1"/"DBS2"/"WAS1"/"WAS2" that don't exist anywhere in this tenant's real agents) and after (correctly referenced the real most-recent cluster's actual rule name, "DPI Response Action"). Fixed by resolving `cluster_id<=0` to the tenant's most-recently-active cluster before building the prompt.
21. **Threat Intel** (`/threat-intel`) — `threat-intel.spec.ts`. IOC/Feed/Actor CRUD (add/edit/delete/toggle/sync) all real, no fabrication found there. `api/intel_enterprise.go` had 4 separate real bugs: the "Tactics Mapped" KPI (`covered_tactics`) was computed by lowercasing the first 4 characters of each MITRE technique ID and checking whether that substring appeared inside a hardcoded English tactic name — technique IDs and tactic names share no characters by construction, so this never matched anything (verified live: 45 real techniques, 0 covered_tactics, always) — `sigma_rules` already has a real `mitre_tactic` column, so replaced the broken heuristic with `COUNT(DISTINCT mitre_tactic)` (now correctly 10). The relationship graph's campaign→IOC edges joined a nonexistent `ioc_blocks` table — the *third* occurrence of this exact missing-table bug this phase (also found on NBA and Behavioral Detection) — fixed with a time-correlation heuristic against real IOC hits, matching this file's own already-honest "heuristic" label elsewhere (this tenant has zero real IOC hits yet, so edges are correctly still empty — verified the fix is structurally sound via direct SQL, not just an unchanged empty result). The search endpoint's alert-matching and the AI panel's alert-context enrichment both referenced `alerts.source_ip` — the same column already found not to exist 3 times over on the Alert Clusters page this phase, now a 4th and 5th occurrence — every search's "Matching Alerts" section and every AI analysis of an IP indicator silently got zero alert evidence; verified live before/after (searching "c2" against real "C2 Beacon Detected" alerts went from 0 results to 2) and fixed both the same way (join `agents` for the real `ip_address`).

22. **Threat Actors** (`/threat-actors`) — `threat-actors.spec.ts`. `PostActorResponse`'s "Block IOCs" action was already fixed ahead of time during the Threat Intel pass (the `ioc_blocks`-doesn't-exist bug). This pass found the single highest-impact bug of the phase by query count: `TagAlertWithActors` — the sole function populating `actor_alert_tags`, which every other endpoint on this page (campaigns, exposure, detection coverage, relationships, timeline, analytics) is built on — matched an alert's `mitre_technique` against `threat_actors.mitre_techniques` with an exact `=ANY(...)` comparison. Both columns independently mix base ("T1078") and sub-technique ("T1071.001") MITRE ID forms, so the exact match silently missed ~2/3 of real correlations in this tenant's data (54 exact matches vs. 174 once sub-technique suffixes are ignored, verified via direct SQL before the fix) — fixed to match on the base technique ID and backfilled this tenant's existing tags to the corrected set. The identical bug shape existed client-side in `KillChainView`'s "N of 8 stages covered" calculation (exact Set membership instead of base-technique matching) — fixed the same way. Also found `threat_actors` has no unique constraint at all, and `cmd/seed/demo`'s `seedThreatActors` used a bare `ON CONFLICT DO NOTHING` with no real target to trigger on, so re-running the demo seeder duplicated its 3 actors (confirmed via two sets of identical rows ~2h20m apart) — fixed with an explicit existence-check guard and cleaned up the pre-existing duplicates. The Hunt tab showed 6 "hunt type" buttons but the backend only ever differentiated 2 of them (the other 4 — Logs/DNS/Firewall/Cloud — fell through to an undifferentiated generic case), and a `queries` field computed for the IOCs hunt type was never rendered anywhere — trimmed the button set to the 2 real types, pointed to the already-real AI-powered Hunt Guide for the rest, and rendered the previously-dead field. Minor cleanup: removed a wasteful dead-code duplicate query in `PostActorAI` that always errored (scanning array columns into an invalid `(*strings.Builder)(nil)` destination) and was silently discarded.
23. **Sigma Rules** (`/sigma-rules`) — `sigma-rules.spec.ts`. Genuinely one of the most sophisticated real engines in the codebase: a real recursive-descent condition parser (correct NOT/AND/OR precedence, "1 of"/"all of"/wildcard quantifiers), a real keyword-matcher with the full Sigma modifier chain (base64/base64offset/windash/utf16le/utf16be, contains/startswith/endswith/regex/cidr/numeric), and a real multi-document YAML importer — all with existing passing unit tests, no fabrication found in dashboard/analytics/MITRE-coverage/categories/relationships. Real bugs found: `PostSigmaBulk` (enable/disable/delete/set_severity/set_status) wrote directly via `database.DB.Exec`, bypassing the services layer every other mutation path uses — so it never called `services.InvalidateSigmaCache`, meaning a bulk-disabled rule could keep firing for up to the 30s cache TTL (verified live: a bulk-disabled rule vanished from the enabled set immediately once fixed). The Import/Export tab's UI explicitly promises "duplicates skipped", but `ImportSigmaYAML` had zero duplicate-title checking (`sigma_rules` has no unique constraint) — verified live that importing the same file twice created 2 rows while both responses claimed `skipped: 0`; fixed with an existence-check. `runBulk`'s toast built `${bulkAction}d`, producing literally "3 rules set_severityd" for the two non-enable/disable/delete actions — fixed with a label map. Found a second, more interesting bug while writing that same test: the Bulk Ops "Value" `<select>` starts from state `''` which matches no `<option>`, so the browser visually shows the first option ("critical") selected while React's state stays empty — a real user who trusts the display and clicks Apply without touching that dropdown gets a silent, unexplained "Bulk action failed" (backend correctly 400s "value required"); fixed by syncing state on Action-dropdown change and surfacing the real backend error message. `StatusBadge` also had a `'test'` vs `'testing'` color-check mismatch (cosmetic, on the Dashboard's own status labels only). **Found and fixed one bug entirely outside this page's scope** while grepping every write to `sigma_rules` for the missing-cache-invalidation class: Threat Actors' `PostActorResponse` "Create Sigma Rule" action (already reviewed last pass) always 500'd — `sigma_rules` has no `updated_at` column, and `keywords` is `jsonb`, not an array type Postgres can implicitly cast a bare `ARRAY[...]` into. Fixed to match the real schema (verified live: a real row is now created instead of a 500) and added the same missing cache invalidation; added a regression test to `threat-actors.spec.ts`. Deferred, not fixed: `components/SigmaImportButton.tsx` is a complete, working, real drag-and-drop import component that is never imported/used anywhere in the app — orphaned dead code, left alone since it isn't broken and might be scaffolding for a not-yet-built page (e.g. Hunt Workbench). **Revisited during the YARA Rules pass**: a fully-built "Graph" tab (real `RelGraph` component + the real `GetSigmaRelationships` endpoint already vouched for above) was never added to the `TABS` array or given a render block — completely unreachable despite being entirely real underneath. Fixed by wiring it in; added a regression test.
24. **YARA Rules** (`/yara-rules`) — `yara-rules.spec.ts`. Architecturally different from Sigma: matching happens on the *agent* (it downloads enabled rules and scans locally with real libyara, confirmed end-to-end via `xcloak-agent-desktop/agent/executor.go`'s `scan_yara` case), so there's no server-side rule cache to invalidate — the "Scheduled YARA Scan" feature (a real scheduled task with `task_type: scan_yara`) is genuinely wired end-to-end, confirmed by reading both the scheduler dispatch and the agent executor. Real bugs found: `yara_matches` has no `rule_id` column — matches correlate to a rule purely by `rule_name` text (inherent to how YARA reports matches), and `UpdateYaraRule` let a user freely rename a rule with zero warning, silently orphaning all of that rule's match history from its own detail page (verified live: `match_count` dropped from 1 to 0 immediately after a rename). Fixed by cascading the rename to `yara_matches.rule_name` inside the same transaction. The exact same orphaned "Graph" tab bug found on Sigma Rules (see above) also existed here — fixed identically. The Matches tab's expanded-row detail had two dead buttons: "Create Alert" (misleading — `SaveYaraMatches` already auto-creates a real alert for every match, so this implied a manual step that already happened) removed; "Quarantine File" mapped to a real, already-used response-action type with no endpoint on this page to call it — added `POST /api/yara/matches/:id/respond` reusing the same destructive-task/pending-approval mechanism Incidents/Alerts already use, verified live that a real `agent_tasks` row is created.
25. **JA3 Fingerprints** (`/ja3-fingerprints`) — `ja3-fingerprints.spec.ts`. Better-defended data model than Sigma/YARA: `ja3_fingerprints` has a real `UNIQUE (hash, COALESCE(tenant_id,0))` index with a genuinely working `ON CONFLICT ... DO UPDATE` upsert, so no duplicate-creation bug here; detection (`services/ja3_detector.go`) always does a fresh DB query rather than caching, so the missing-cache-invalidation class doesn't apply either; and the Relationships tab (real `RelGraph` + real `GetJA3Relationships`) was already correctly wired into `TABS` and the render section, unlike the identical-looking gap just fixed on both Sigma Rules and YARA Rules. Real bugs found, all in `api/ja3_enterprise.go`: `GetJA3Relationships`'s IP-node/IP-edge queries and `GetJA3FingerprintDetail`'s connections query all referenced a column `remote_addr` that has never existed on `endpoint_connections` (the real column is `remote_address`) — confirmed live via psql reproducing the exact error — both silently empty forever since the query error was discarded. The FingerprintDetail query also independently referenced `ec.created_at`, which doesn't exist either (real column: `collected_at`) — a second wrong-column bug compounding the first. Beyond the column names, that query also had no correlation to the specific fingerprint being viewed at all — fixed by scoping it to the agent IDs that actually appear in this fingerprint's own alert list, verified live end-to-end with a synthetic alert + real seeded connections. Also found `analyticsData`/`timelineData` — both fetched from real, working backend endpoints (`GetJA3Analytics`/`GetJA3Timeline`) on tab switches — were never rendered anywhere in the component; wired both in with compact new panels (Library tab's "Match Trend / Most Recently Active", AI tab's "Top Matched Fingerprints"), reusing the existing `SparkTrend` component already used on the Dashboard tab. Confirmed, not fixed: the demo tenant's seeded `endpoint_logs.parsed_fields` is empty for every row (the same cross-page gap flagged during Live Logs), so the real JA3 detector has no live traffic to match against for this tenant currently — a demo-data completeness issue, not a page-specific code bug.
26. **Hunt Workbench** (`/hunt-workbench`) — `hunt-workbench.spec.ts`. Response actions (isolate_host/kill_process/quarantine_file/block_ip/create_incident) were the same 100%-fake-button pattern as UEBA/Insider Threat/Network Map/Attack Paths — now dispatch real agent tasks through the shared pending_approval gate, or create real IOC/incident rows. IOC hunt for IP/domain types silently returned nothing (the recurring `endpoint_connections` wrong-column bug — `remote_addr`/`created_at` instead of `remote_address`/`collected_at` — already fixed on JA3 Fingerprints 3 times this phase). KQL hunt findings always showed a blank hostname (`models.Log` has no `Hostname` field) — resolved via a batched agent lookup. **Found the phase's most severe bug while testing this page, unrelated to the page's own fixes**: `singleRow.Next()` in `hunt_workbench_service.go` was hardcoded `return true`, so `scanHuntRuns`'s loop never terminated when scanning a single-row result (used by `GetHuntRun`, called on every hunt-run status poll) — one request would spin forever appending empty structs, running backend RSS from ~50MB to multiple GB in under a minute. This was the actual cause of 3 separate kernel OOM kills that day, which had been taking down the whole VS Code session — not anything Docker/Ollama-related as first suspected. Reproduced live with pprof (`scanHuntRuns` was 92% of allocated heap), fixed, verified the exact test that previously crashed the backend now passes in <1s with flat memory. **Also added lasting infra hardening as a result**: `debug.SetMemoryLimit()` + opt-in pprof in `main.go`, hard-cgroup-capped dev wrapper scripts for both backend and frontend dev servers, and migrated Kafka off Zookeeper to KRaft mode (removes a whole JVM per environment, ~380MB saved) after confirming production's `docker-compose.yml` was already properly memory-capped on every service.

27. **Threat Hunt** (`/hunt`) — `hunt.spec.ts`. A separate, structured hunt-library feature distinct from Hunt Workbench (different data model — `threat_hunts`/`threat_hunt_findings` vs. `hunt_templates`/`hunt_runs`). The MITRE tab called Hunt Workbench's own `mitreCoverage()` endpoint — scoped entirely to Hunt Workbench's tables — so a user's own threat hunts here never affected what this tab showed (confirmed live: `threat_hunts` didn't even exist yet for this tenant, lazily created on first write, while `hunt_templates` already had 20 rows); added a real `GetThreatHuntMITRECoverage` scoped to this page's own tables, extracting the shared MITRE matrix into `api/mitre_matrix.go` so both endpoints render against the same tactic set without duplicating it. `PostThreatHuntResponse` was the same 100%-fake-button pattern found repeatedly this phase — fixed `isolate_host`/`block_ip`/`block_ioc`/`open_incident`/`open_case` with real dispatch; `run_soar`/`hunt_similar` didn't fit the modal's single-target shape at all, replaced with real distinct capability instead (a client-side "Hunt Similar" that reruns an ad-hoc search on the finding's own IOC value, and a real link to Playbooks). The Workspace tab's "Respond" button conflated the *hunt's* ID into `finding_id`, while the Findings tab's own Respond button never sent `hunt_id` at all — both fixed by tracking them as separate state. **Found a bug identical in shape on Hunt Workbench too, predating this phase's Hunt Workbench pass**: both pages' response-action audit-log INSERTs referenced `user_id`/`resource_type`/`resource_id` — none of which exist on `audit_logs` (real columns: `tenant_id`/`action`/`details`/`created_at`/`username`/`ip_address`) — so every response action's audit trail silently failed to write, forever, on both pages; fixed both to use the already-correct `repositories.CreateAuditLog` helper every other real audit write in the codebase already uses. The entire "Automation" tab was fabricated — 8 hardcoded "trigger → action" rules with fake active/inactive badges and zero backend backing anywhere — removed entirely, same as Log Sources' fake Alerts tab earlier this phase. Also fixed: executing a hunt (Library's inline Play button or Workspace's Execute Now) never refreshed the Library list/dashboard cache, so hit_count/last_run_at/success_rate went stale until an unrelated filter change; and `addComment` appended a fake `Date.now()` client-side id instead of the real one the backend already returns.
28. **DFIR** (`/dfir`) — `dfir.spec.ts`. A large page (30 backend endpoints) — most of it genuinely well-built: real evidence/custody/timeline/process-tree/network-forensics queries, real LLM-backed file/malware/memory analysis and reporting, an honest manual step-through Timeline Replay (not automated, correctly not flagged). `PostDFIRResponse` was the starkest instance of the 100%-fake-button pattern found yet — it validated nothing and always returned canned success with a literal source comment admitting it was a stub ("In production: route to agent command queue or SOAR") — fixed with real `isolate_host`/`kill_process`/`quarantine_file`/`block_ip`/`open_incident`/`open_case` dispatch. `kill_process`/`quarantine_file` needed a second `target_host` field beyond `target` (a PID or file path isn't a hostname) that the single-field modal never had — added it, and fixed the Process Tree's "Kill" button, which had been prefilling the target with `node.process_name` (not even unique, and not what `kill_process` expects — a numeric PID) while never capturing which host the process was even on. `delete_file` was offered as an action with zero backing anywhere — the real agent executor only supports `quarantine_file` (move, not delete); `run_soar` needed a playbook picker this modal never had — both removed, replaced with a real link to `/playbooks`. `GetDFIRArtifacts` queried `endpoint_logs.created_at` — the same recurring wrong-column bug found repeatedly this phase (real column: `collected_at`) — silently zeroing the Artifacts sub-tab for every platform/type combination forever; fixed and verified live with a real seeded `cron` log entry. Also removed two fabricated "threat" highlighting heuristics in the Network Forensics table with zero real backing: any remote IP starting with `185.` was painted red (a legitimate hosting range — this actively mislabeled real unrelated seeded traffic, e.g. a real SSH login log entry), and any process name containing `svchost32` (a string appearing nowhere in this codebase's backend or seed data — confirmed via grep — the real process is `svchost.exe`) was painted red too; neither was backed by any real IOC/reputation lookup.

29. **Deception** (`/deception`) — `deception.spec.ts`. The starkest cluster of wrong-table/wrong-column bugs found this phase, on code that genuinely *attempted* real work rather than the usual 100%-fake-button pattern: `PostDeceptionResponse`'s `isolate_endpoint`/`collect_memory` referenced `playbook_tasks`, a table that doesn't exist anywhere in this schema (real: `agent_tasks`) — and both branches gated on `body.AgentID > 0`, which the frontend never sent at all; `block_ip` inserted into the real `firewall_rules` table but with `src_ip` (real: `source_ip`); `create_alert` inserted into the real `alerts` table but with `title`/`description` (real: `rule_name`/`log_message`) — the third occurrence of this exact bug this phase, also fixed identically in `cloud_security_enterprise.go`'s cloud response-action logging; `disable_user` read `body.UserID`, which the frontend also never sent. `collect_memory` had no real backing anywhere (no memory-collection task type in the agent executor) — removed rather than faked. Fixed all of it with real dispatch, a `source_host`/`username` field the frontend now actually populates from the trigger, and an honest "no real account" message instead of false success when `disable_user` matches zero rows. **The single biggest finding on this page**: a completely separate, real, tested detection backend (`api/deception.go` — canary tokens with a genuine public trip-recording endpoint, honeyports wired to the real agent connect-event pipeline) had zero frontend caller anywhere in this codebase, while the page's own Decoys/Honeytokens/Triggers tabs point at a parallel data model with no real detection pipeline behind it at all (confirmed: nothing in the whole backend ever `INSERT`s into `deception_triggers`). User chose to build it out (matching the DPI page's precedent) — added a full "Canary Tokens" tab: token CRUD, live trip history, honeyport management. Verifying that live surfaced one more real bug in the now-reachable code: `TripCanaryToken`'s alert-creation defaulted `agent_id` to `0` when no agent was online, silently violating `alerts`' foreign key on every trip with no online agent (the *common* case — canary tokens exist to catch attackers who aren't running our agent) — verified live that a real trip was recorded but zero alerts were ever created before the fix; `NULL` is a valid `agent_id`, `0` isn't.

30. **Cloud Security** (`/cloud-security`) — `cloud-security.spec.ts`. The cleanest page's code this phase, by a wide margin — read all 23 backend endpoints and all 9 frontend tabs end to end, found zero fabrication and zero wrong-column bugs beyond the `alerts.title`/`description` fix already made during the Deception pass. Two real gaps remained: `PostCloudResponse`'s `block_ip` action had no case in the switch at all — it fell through doing nothing while still reporting 200 success — fixed with a real enabled IOC (this domain has no real cloud firewall API to enforce against, matching every other page's block_ip this phase); `disable_iam_user` only ever checked a numeric `identity_id`, but its one real caller (a `cloud_threats` row, which only carries a `source_user` name string, not an id) never had one to send, so it silently did nothing on every real invocation — fixed to also resolve by name, with an honest message when no matching identity exists (the common case, since a threat's source user is often external/unmanaged). **The bigger issue wasn't a bug in the handlers at all**: none of this page's own dedicated tables (`cloud_accounts`/`cloud_assets`/`cloud_findings`/`cloud_identities`/`cloud_threats`/`cloud_drift_events`) had any seed data anywhere — confirmed live, the tables didn't even exist yet — because unlike the parallel `seedCloudInfraAlerts` (which only ever writes generic `alerts` rows for a different feed), nothing populated this page's own domain. Added `seedCloudSecurity` to the demo seeder (3 accounts, 10 assets, 10 findings, 8 identities, 6 threats, 4 drift events, with denormalized `cloud_accounts` counters computed from real child rows) — verified idempotent on a second run, following the lesson from Threat Actors' duplicate-seeding bug earlier this phase.

31. **Email Security** (`/email-security`) — `email-security.spec.ts`. This phase's largest fabrication footprint by response-endpoint count: `PostEmailResponse` was a pure canned-message lookup table — every action (quarantine/delete/block_sender/block_domain/block_url/block_hash/reset_password/create_incident, and any unrecognized action string) returned "Action executed" with zero real side effect. Rewritten to perform real work: quarantine/delete really `UPDATE email_messages`; block_* create real enabled IOCs via `repositories.CreateIOC` (block_hash auto-typed via `services.GuessIOCType`); `reset_password` calls the real, existing, anti-enumeration `services.RequestPasswordReset`; `create_incident` does a real `INSERT INTO incidents`; an unrecognized action now 400s instead of lying. Three GET endpoints were also fully fabricated — user chose "build real backing where possible" over removing them: `GetEmailAuthResults` computed spf/dkim/dmarc rates as fixed 78/82/71% of total regardless of any message's real result, plus a hardcoded 7-domain table (including a literal fake `suspicious-bank.xyz`) — rewritten to aggregate real per-message `spf_result`/`dkim_result`/`dmarc_result` columns and a real per-sender-domain `GROUP BY` query; ARC/BIMI dropped entirely (no per-message signal exists for either) rather than kept fake — the frontend had independently hardcoded `ARC: 42, BIMI: 31` and a literal "29% of inbound emails fail DMARC" string, both removed too. `GetSenderIntelligence` used a crude domain substring match (`"xyz"` → malicious) and returned fully invented WHOIS/GeoIP/ASN/domain-age fields — rewritten to derive reputation from real IOC hits + real `email_urls` malicious-verdict counts; the fabricated fields were removed from the response rather than kept fake. `GetEmailThreatIntel` returned four hardcoded arrays (malicious domains/IPs, malware families, threat actors) identical for every tenant — rewritten to derive `malicious_domains` from real `email_urls` and `threat_actors` from the real, previously-never-aggregated `email_campaigns.threat_actor` column; `malicious_ips`/`malware_families` removed (no real source-IP or structured malware-family field exists anywhere in this schema). Also found: the Response tab's free-text action form only ever sent `sender`/`domain`/`url`/`hash`, silently dropping `message_id`/`email` — meaning quarantine/delete/reset_password could never have worked from the UI even after the backend fix; fixed to send all target fields. A `run_soar_playbook` action existed with zero backend implementation (the same fake-SOAR-action pattern already fixed on Threat Hunt/DFIR) — replaced with the same real `<a href="/playbooks">` link used on those pages. **Like Cloud Security before it**: all 7 of this page's own tables (`email_messages`/`attachments`/`urls`/`campaigns`/`user_risk`/`reported`/`policies`) had zero seed data anywhere in a fresh dev environment — added `seedEmailSecurity` to the demo seeder, idempotency-guarded like every other keyless table this phase; verified a second run doesn't duplicate rows.

32. **Containers / K8s** (`/container-security`) — `container-security.spec.ts`. A fabrication footprint rivaling Email Security's, spread across nearly every tab — confirmed the three largest/most novel judgment calls with the user via `AskUserQuestion` before touching them, fixed the rest directly as clear recurrences of patterns already established this phase. `PostContainerResponse` was a pure canned-message dispatcher (7 actions, zero real effect, any action string accepted) — rewritten: `kill_container`/`delete_pod` do real UPDATE/DELETE against `k8s_pods`, `quarantine_node` does a real UPDATE against `k8s_nodes`, `block_image` creates a real enabled IOC (type=image) — matching every other page's "real DB write standing in for a live enforcement API that doesn't exist" pattern; `revoke_service_account`/`scale_deployment` now honestly 501 (no real IAM/deployment abstraction exists anywhere in this schema) instead of claiming fake success; `run_soar_playbook` removed and replaced with the same real `/playbooks` link used on Threat Hunt/DFIR/Email Security. Per the user's confirmed choices: `GetContainerCompliance`'s fully fabricated 5-framework grid (CIS/NSA/PCI/NIST/ISO27001, identical for every tenant, no real per-framework K8s control catalog exists anywhere) was replaced with a real summary computed from `k8s_admission_violations` (severity breakdown, denied/allowed counts, real violation rows); `GetContainerAttackPaths` — a fully static graph with specific fictional resource names ("web-app Pod", "db-credentials Secret") that could be mistaken for real discovered assets — was replaced with a real "Top Risk Factors" panel built from actual privileged/root/host-network pods and actual RBAC excessive-permission findings; `GetContainerThreatIntel`'s `threat_actors`/`malware_families` (zero real attribution data anywhere in this schema, unlike Email Security's `email_campaigns.threat_actor` which already existed) were removed rather than half-faked. Found and fixed directly (clear real-data-one-query-away shape, same as NBA's namespace_risk duplication earlier this phase): `GetSupplyChain`'s hardcoded `trusted_registries` replaced with a real per-registry aggregation from `k8s_images`; `GetK8sSecrets`'s `plaintext`/`expired`/`exposed` fields — permanently hardcoded to 0 with zero real computation, misleadingly implying "verified none exist" — removed rather than left as fake-honest zeros; `GetContainerThreatIntel`'s `malicious_images`/`ioc_matches` wired to a real, previously-unused `k8s_images.malware_found` column and the real `iocs` table respectively; `GetContainerAnalytics`'s `top_vulnerable_images`/`alert_by_type`/`namespace_risk` were hardcoded despite a real equivalent query being one line away — wired to real aggregations. **Like Email Security and Cloud Security before it**: none of this page's 8 own tables had any seed data anywhere in a fresh dev environment — added `seedContainerSecurity` to the demo seeder, idempotency-guarded like every other keyless table this phase; verified a second run doesn't duplicate rows.

33. **AD Attacks** (`/ad-attacks`) — `ad-attacks.spec.ts`. A fabrication footprint at least as large as Container Security's — nearly every intelligence/exposure/assessment/graph endpoint was fully or mostly fake, glued onto a handful of real `COUNT()` queries. Confirmed the two largest/most novel judgment calls with the user via `AskUserQuestion`; fixed the rest directly, including two fake graphs with the exact same shape as Container Security's already-approved fix. `PostADResponse` was a pure canned-message dispatcher (7 actions, zero real effect, any action string accepted) — rewritten: `disable_user`/`reset_password`/`remove_group_membership`/`disable_service_account` do real UPDATEs against `ad_users`; `force_ticket_renewal`/`isolate_endpoint` now honestly 501 (no real Kerberos KDC/EDR integration exists); `run_soar_playbook` removed, replaced with the real `/playbooks` link. Per the user's confirmed choices: `GetADAssessment`'s hardcoded "AD Hygiene Score: 61%" and 10 always-`"fail"` checks (7 with zero real backing, 1 with a wrong-variable bug reusing `passwordNeverExpires` for an unrelated "inactive privileged accounts" claim) replaced with a real score computed from only the 4 checks with genuine backing; `GetADThreatIntel`'s `threat_actors`/`malware`/`credential_campaigns` (zero real per-tenant attribution data anywhere) removed, `ioc_matches` wired to the real `iocs` table. Fixed directly as a confirmed recurrence of Container Security's Attack Path decision: `GetADAttackPaths` and `GetADRelationshipGraph` were both fully static graphs with specific fictional entity names (svc_backup, hcraig, administrator, jsmith) reused across multiple endpoints — replaced with real top-risk identities/computers and a real graph built from actual admin users, DCs, and unconstrained-delegation computers. Also fixed directly (same "real data one query away" shape as Container/NBA before it): `GetADTiering`'s fake "AD Admin Workstations: 2"/"Tier-0 Groups: 3"/"Member Servers: 8"/hardcoded "Standard Users: 847" removed or fixed to real queries, and its `privileged_sessions` array — which recomputed fake session start times fresh on every request, silently drifting forward in real time, with zero real session-tracking data anywhere — removed entirely; `GetADExposure`'s real `unconstrained_delegation` count had an arbitrary "+2" offset and a fake affected-hosts list, fixed to the real count and real affected computer names, while its other 6 findings (weak ACLs, anonymous LDAP, legacy protocols) had the same zero-real-backing shape as the just-approved Assessment checks and were dropped for the same reason; `GetADAnalytics`'s `top_failed_logins` was hardcoded despite a real equivalent query being one line away. Also found and fixed: a client-side-only "AI Insights" panel with 3 fully hardcoded findings (zero backend call, specific fictional entities) sitting right next to the page's genuinely real AI Analysis panel — removed, matching the Alerts page's "fake content beside real content" fix; 3 completely dead buttons (Open Timeline/Start Hunt/Log Search, zero onClick) — wired to real destination pages, matching the Live Logs precedent. **Like Email/Container Security before it**: none of this page's 8 own tables had any seed data anywhere in a fresh dev environment — added `seedADSecurity` to the demo seeder, idempotency-guarded like every other keyless table this phase.

34. **Supply Chain** (`/supply-chain`) — `supply-chain.spec.ts`. One of the cleanest backends found this phase, on par with Cloud Security — several endpoints already carried explicit "no real X exists in this schema, report an honest empty/zero result" comments from an earlier pass (`GetSCCodeIntegrity`, `GetSCThirdPartyRisk`'s `ci_plugins`, `GetSCThreatIntel`'s `campaigns`), and `GetSCThreatIntel`'s malicious-package/exploited-CVE lists are genuine real-world public incident data (event-stream, node-ipc, Log4Shell, etc.) used only as real per-tenant match criteria against this tenant's actual dependencies rather than fabricated per-tenant claims — verified live that `ioc_matches` only appears when a real matching dependency row exists. `GetSCCompliance` is also fully real, backed by the shared `fce_frameworks`/`fce_controls` tables the Dashboard and Risk Posture pages already use. The one real bug: `PostSCResponse` was the familiar pure canned-message dispatcher (6 actions, zero real effect, any action string accepted) — rewritten: `block_build`/`disable_pipeline` do real UPDATEs against `sc_build_pipelines` (different status values so the two stay distinguishable); `quarantine_artifact` resolves the target's real `artifact_hash` and creates a real enabled IOC from it, matching every other page's `block_*` pattern this phase; `create_incident` does a real `INSERT INTO incidents`; `create_issue` now honestly 501s (no real GitHub/GitLab API integration exists anywhere in this codebase) instead of claiming a fake issue was filed; `trigger_soar` removed, replaced with the real `/playbooks` link. **Like every other page this phase**: none of this page's 8 own tables had any seed data anywhere in a fresh dev environment — added `seedSupplyChainSecurity` to the demo seeder, idempotency-guarded like every other keyless table this phase, including a real `event-stream` dependency deliberately seeded to exercise the real IOC-match path above end-to-end.

35. **OT / ICS** (`/ot-ics`) — `ot-ics.spec.ts`. A fabrication footprint comparable to Container/AD Attacks — nearly every risk/topology/attack-paths/compliance/analytics endpoint had at least some hardcoded content mixed with real `COUNT()` queries. Given how closely the shapes matched patterns already reviewed and approved 2-3 times each this phase, fixed directly as confirmed recurrences rather than re-asking. `PostOTResponse` was the familiar canned-message dispatcher — rewritten: `create_incident` does a real `INSERT INTO ot_incidents`; `block_network_path`/`escalate_emergency` — both already flagged "requires operator approval" in the old fake code — now create a real row in the existing, fully-built Approval Queue system (`aq_requests`/`aq_audit`, the same tables backing the real `/soar-approvals` page) instead of just claiming to; `notify_operators`/`capture_traffic` now honestly 501 (no real operator-alarm/PCAP-capture integration exists anywhere); `run_soar_playbook` removed, replaced with the real `/playbooks` link. `GetOTCompliance`'s hardcoded IEC 62443/NERC CIP/NIST SP 800-82 grid replaced with real data from the shared `fce_frameworks`/`fce_controls` tables — the identical fix shape as Supply Chain's `GetSCCompliance`. `GetOTAttackPaths`'s two fully static attack chains (reusing fictional device names from other fake endpoints on this same page) replaced with real top-risk-asset and exposed-control-CVE panels, mirroring Container/AD's Attack Path fixes. `GetOTRiskAssessment`'s `weak_auth`/`open_services` fields were declared but never queried (always silently 0) — removed; `critical_assets`/`findings` were fully hardcoded — rewired to real asset-risk-derived data. `GetOTBaseline`'s `categories` had fixed fake learned-item counts, and `deviations` recomputed fake timestamps fresh on every request (silently drifting forward in real time) — the same "ever-refreshing fabrication" bug class first found on AD Attacks' `privileged_sessions` — both replaced with real `ot_baselines`/`ot_alerts` data. `GetOTThreatIntel`'s `ioc_matches` (2 hardcoded fake IOCs) rewired to a real match against this tenant's actual `iocs` table, exactly like Supply Chain's fix; `ot_threat_actors`/`industrial_malware`/`sector_advisories` are genuine real-world ICS threat history (Stuxnet, TRITON, Industroyer) kept as advisory reference content. `GetOTAnalytics`'s `most_active_plcs`/`protocol_distribution`/`config_changes_7d` were hardcoded despite real equivalent queries one line away — wired to real aggregations; `firmware_age` had no real per-asset install-date field anywhere in this schema to back a 4-bucket breakdown, so it was removed rather than forced through a fake proxy. `GetOTTopology`'s `links` and `GetOTProtocols`'s `sessions` — both hardcoded fake traffic samples with fixed IPs — wired to real `ot_traffic` aggregations. **Also fixed a bug in this session's own earlier work**: both this page's and Supply Chain's `execute()` handlers had a bare `catch { setResult({ error: 'Action failed' }) }` that discarded the real backend error message — meaningful now that both pages have real 501 responses to surface — both fixed to read `err.response.data.error`. **Like every other page this phase**: none of this page's 8 own tables had any seed data anywhere in a fresh dev environment — added `seedOTICSSecurity` to the demo seeder, idempotency-guarded like every other keyless table this phase.

36. **Process Injection** (`/process-injection`) — `process-injection.spec.ts`. `GetPIModules`/`GetPIHandles` were fully hardcoded (zero DB query, not even `tenant_id` used) with fictional processes — no agent capability collects per-process loaded-module or handle-table data anywhere in this codebase, so both now report an honest empty list. `GetPIBehavioral`'s 6 hardcoded parent/child "detections" were rewritten to derive from real `pi_injections` rows — an injection's real src/dst process pair already is the behavioral signal, no separate table needed. `GetPIThreatIntel`'s `malware_matches` implied real per-tenant detections (specific sha256 + "target: explorer.exe") rather than generic reference content, crossing further into fabricated-detection territory than Container/Supply Chain/OT's kept real-world advisory lists — rewritten to a real match between `pi_processes.sha256` and the real `iocs` table, the same pattern established on Supply Chain/OT; `threat_actors`/`campaigns` (the latter recomputing a fake timestamp on every request) removed since no real per-tenant attribution data exists anywhere. `GetPIMITREMap`'s sub-technique `detected`/`count` fields were fixed for every tenant — rewritten to a real `GROUP BY mitre_technique` count. `GetPIAnalytics`'s 4 hardcoded arrays wired to real aggregations over `pi_injections`/`pi_api_calls`. `PostPIResponse` was the familiar canned-message dispatcher — rewritten: `kill_process`/`collect_process`/`isolate_endpoint` resolve a real agent by hostname and dispatch a real pending-approval task (all three confirmed-real agent executor capabilities); `suspend_process`/`dump_memory` now honestly 501 (no real agent capability exists for either, confirmed against the executor's full task-type list); `run_soar` removed, replaced with the real `/playbooks` link. Also found and fixed on the frontend: the Response tab's action buttons sent hardcoded fallback target/pid/hostname values (`'explorer.exe'`, `4512`, `'WS-ANALYST-01'`) whenever no real injection had been fetched yet, meaning a response action could silently fire against entirely fictional identifiers — fixed to disable the actions with an honest message instead; `respond()` also had no error handling at all — fixed to surface the real backend error. **Like every other page this phase**: none of this page's 5 own tables had any seed data anywhere in a fresh dev environment — added `seedProcessInjection` to the demo seeder, idempotency-guarded like every other keyless table this phase, including a real IOC fixture matching a seeded process hash to exercise the real malware-match path end-to-end.

---

37. **Defense Evasion** (`/defense-evasion`) — `defense-evasion.spec.ts`. Confirmed one large judgment call with the user: a whole standalone "Detection Validation" tab (4 metric cards + 2 tables — detection success rate, missed attempts, false positives, avg time-to-detect, per-platform coverage, per-category technique coverage) was 100% hardcoded with genuinely zero possible real backing (no purple-team/atomic-red-team exercise tracking exists anywhere in this schema, and building one would be a new feature, not a bug fix) — removed the whole tab: `GetDEValidation` handler, its route, its API client method, and the tab/view. Everything else fixed directly as clear recurrences of patterns already established this phase: `GetDEBehavioral`'s 5 hardcoded detections (reusing fictional hostnames found elsewhere on this page) rewritten to derive from real `de_events` rows directly. `GetDEThreatIntel`'s `malware_families`/`ioc_matches` implied real per-tenant detections (specific confidence scores, a fake sha256, a fake registry key) rather than generic reference content; `threat_actors`/`campaigns` (the latter recomputing a fake timestamp on every request — the 4th occurrence of this bug class this session) had zero real backing anywhere — all four removed, replaced with a real technique-frequency breakdown from `de_events`. `GetDEMITRE`'s sub-technique `detected`/`count` wired to a real `GROUP BY mitre_id` count. `GetDEAnalytics`'s 4 hardcoded fields wired to real aggregations over `de_events`/`de_controls`. `PostDEResponse` was the familiar canned-message dispatcher (8 actions!) — rewritten: `restart_security_services`/`reenable_defender`/`restore_firewall` do real UPDATEs against `de_controls`; `isolate_endpoint`/`kill_process` resolve a real agent and dispatch a real pending-approval task; `create_incident` does a real `INSERT INTO incidents`; `collect_memory` now honestly 501 (no real agent capability, matching Process Injection's identical finding); `run_soar` removed, replaced with the real `/playbooks` link. Also found and fixed a more severe variant of a bug from this session's own earlier work: the Response tab's `respond()` call sent hardcoded fake identifiers for literally every action with zero target-selection UI at all — worse than Process Injection's version — fixed by adding real hostname/target inputs. Separately discovered the API client's `respond`/`analyzeAI`/`generateReport` methods on this page (and, found while fixing this, **Process Injection's identical methods from earlier in this same session**) had a `.catch(() => ({ data: null }))` wrapper silently swallowing real errors before they ever reached the component — meaning the component-level error handling added to Process Injection earlier was dead code. Fixed both pages' API client methods and added matching component-level `try/catch`. **Like every other page this phase**: none of this page's 4 own tables had any seed data anywhere in a fresh dev environment — added `seedDefenseEvasion` to the demo seeder, idempotency-guarded like every other keyless table this phase.

38. **Cases** (`/cases`) — `cases.spec.ts`. **The highest-severity bug of the whole phase**: two independent implementations of a table both named `cases` — the real, properly-migrated one (`repositories/case_repository.go`, also used by the Executive dashboard and a background SLA-breach checker) and `api/cases_enterprise.go`'s own `createCasesTables()`, which `CREATE TABLE IF NOT EXISTS`'d a completely different, fictional schema (`case_id`/`priority`/`owner`/`team`/`due_date`/`sla_status`/`linked_incidents`/`linked_alerts` — none of which exist on the real table) that silently no-op'd against the real one. Every handler on this page queried columns that never existed; confirmed live via psql that `SELECT case_id FROM cases` fails outright. Most query errors were silently swallowed (`if err == nil { ... }` patterns, an unchecked `.Scan(&id)` on every case creation), so the entire page appeared merely empty in a fresh dev environment rather than visibly broken — this is a different, more severe bug shape than this phase's usual "fabricated display data": a genuinely real, cross-page-dependency-bearing feature was completely inert. Confirmed the fix approach with the user via `AskUserQuestion` (rewrite queries to the real schema + add `tags`/`template` via defensive `ALTER TABLE ADD COLUMN IF NOT EXISTS`, matching Email Security's precedent for concepts with real frontend usage but no real column) rather than swapping route wiring to the real `cases.go`'s parallel (correct but frontend-incompatible) handlers. Rewrote every handler: `case_id`/`evidence_id` now derived from the real `id` instead of stored; `sla_status` derived from real `sla_breached`/`sla_breach_at` via a new `caseSLAStatus()` helper; `owner`→`assigned_to_name`; `due_date`→`sla_breach_at`; `team` dropped entirely (zero display usage anywhere, confirmed via grep); `linked_incidents`/`linked_alerts` dropped (the real `case_alerts` junction table already exists for this but isn't surfaced in this page's UI); comments/evidence rewritten to real `case_comments`/`case_evidence` columns (`body`/`username`/`is_system`, `evidence_type`/`title`/`description`/`added_by`/`added_by_name` + the new evidence columns) with real `JOIN cases` tenant-scoping since neither table has its own `tenant_id`, and now use the real authenticated user server-side instead of trusting client-supplied `author`/`is_internal`. `GetCasesEnt`/`PostCase`/comments/evidence now return real errors instead of silently empty/false-success. `DeleteCaseEnt` now also cleans up `case_tasks`/`case_notes`/`case_timeline`, which have no real FK cascade unlike `case_alerts`/`case_comments`/`case_evidence`. Also fixed: `applyTemplate()` only ever pre-filled the create form for display — selecting a template never created its task checklist; `PostCase` now auto-creates real `case_tasks` rows from the template's task list when a `template` is supplied. Also found and fixed the same `.catch(() => ({ data: null }))` silent-error-swallowing anti-pattern on `casesAPI`'s write methods already fixed on Process Injection/Defense Evasion earlier this phase. Also found `cmd/seed/demo`'s `seedCases` had zero idempotency guard — confirmed live it had silently 18x-duplicated its 3 cases in this dev environment (54 rows, 3 distinct titles) — cleaned up the duplicates, added an existence-check guard, and enriched the 3 seed cases with real `sla_breach_at`/`sla_breached`/`assigned_to_name`/`tags`/`template` values so the newly-real SLA/tags/template/analyst-workload features have real data to exercise. Also found the seeded "investigating"/"resolved" statuses (real, free-text values this schema allows, confirmed via `repositories.GetCaseMetrics`'s own open/closed split) fell through both the frontend's `STATUS_COLOR`/`STATUS_LABEL` maps and the dashboard's 5-bucket status counts — fixed both. Removed a redundant "Priority" field from the create-case form (the real schema has no independent priority column).

39. **Playbooks** (`/playbooks`) — `playbooks.spec.ts`. A visual drag-and-drop SOAR workflow builder (node/edge graph: 10 trigger types, 8 logic ops incl. IF/LOOP/PARALLEL, 12 actions, 5 human-in-loop gates) with zero execution engine behind it — `PostPBExecute` created a `pb_executions` row with `status='running'` and nothing ever completed it, so every real "Execute" click left a permanently-stuck fake-running execution forever, while Dry Run/Versions/most of Analytics/Report were 100% hardcoded fabrication unrelated to the real workflow being edited. Confirmed via `AskUserQuestion`: user chose to build a simplified real executor over leaving execution honestly inert. Built `api/playbook_workflow_engine.go`: walks the workflow's node/edge graph in order (ignoring IF/LOOP/PARALLEL/WAIT/RETRY branching — treated as linear pass-through) and dispatches every reachable action node via a new `target` config field (added to the action config panel, which previously had no way to specify a concrete IP/hostname/username at all). 6 of 12 action types now do real work reusing primitives already built this phase (Block IP/Domain→real IOC, Isolate Endpoint→real agent-task dispatch, Disable User→real `ad_users` UPDATE, Reset Password→real `services.RequestPasswordReset`, Update Firewall→real `firewall_rules` INSERT); the other 6 (Quarantine File/Kill Process — need 2 args a single target field can't carry; Send Email/Create Ticket/Run Script — no real integration exists anywhere) honestly report "skipped" with a specific reason instead of faking success. A non-automatic human-in-loop gate now creates a real `pb_approvals` row and halts the walk there (no resume engine — halting honestly beats silently skipping the gate or hanging forever), finally giving the previously-dead, fully-built Approvals tab (real approve/reject/escalate UI, zero rows ever created) real data. Also fixed: `GetPBVersions` was 100% hardcoded (3 fake versions, ignored the given playbook id) — added a real `pb_playbook_versions` table, populated on every real Publish; `GetPBAnalytics`'s `avg_runtime_s`/`automation_coverage`/`most_used`/`manual_vs_automated`/`failed_steps`/`trend` were all hardcoded (the trend array's dates were also already stale) — wired to real aggregations; `PostPBReport` was a fully fabricated static report (247 executions, 94.7% success, fictional recommendations) — rewritten to real `key_metrics` plus an LLM-generated summary grounded only in those real numbers. Found and fixed a real bug while verifying Approvals live: the new `pb_approvals` rows have a nullable `approver`/`notes` column that were NULL until decided, but `GetPBApprovals` scanned them into plain Go `string` fields — every row silently failed to scan and the list came back empty (verified live: direct SQL returned 2 real rows, the API returned `[]`) — fixed with `COALESCE` in the query. Also removed a whole fabricated "Variables" tab (confirmed via `AskUserQuestion`): claimed to be a "Global Variable Store... secrets encrypted at rest" but was pure client-side `useState` with 8 hardcoded fake variables (including fake masked secrets for a firewall API key, Slack webhook, CrowdStrike tenant ID) and zero backend call anywhere. **Like every other page this phase**: none of this page's 5 own tables had any seed data anywhere in a fresh dev environment — added `seedPBWorkflows` to `cmd/seed/demo/main.go` (4 playbooks with real node/edge workflow graphs, 4 executions spanning success/failed/pending-approval, a real pending `pb_approvals` row, real `pb_playbook_versions` rows, one `pb_schedule`), idempotency-guarded like every other keyless table this phase.

40. **Approval Queue** (`/soar-approvals`) — `soar-approvals.spec.ts`. The human-approval-gate/break-glass system backing `aq_requests`, already used for real by OT/ICS's `block_network_path`/`escalate_emergency` actions (confirmed via grep as the table's only other real caller). **The most severe finding**: several `aq_requests`/`aq_policies`/`aq_audit` TEXT columns have no `DEFAULT` and are nullable, but every GET handler scans them into plain Go `string` fields — any INSERT that omits one (OT/ICS's own direct writes, and the new demo seeder, both do) leaves it `NULL`, and scanning `NULL` into a non-nullable string silently fails the whole row's scan. Confirmed live: `GET /api/aq/queue` returned `[]` despite 10 real rows existing (every row had a `NULL` `case_id`), `GET /api/aq/policies` returned `[]` despite 6 real rows (`NULL` `auto_conditions`), and `GET /api/aq/audit` returned `[]` despite 31 real rows — the latter because **every single `aq_audit` INSERT in the entire codebase omits `ip_address`**, so that column has been `NULL` on every audit row ever created. Fixed at the schema level in `createAQTables()` (`ALTER COLUMN ... SET DEFAULT ''` + a one-time backfill `UPDATE` for existing `NULL`s) across all three tables' affected columns, rather than adding `COALESCE` to every SELECT across this 700-line file. Also fixed: `GetAQDashboard`/`GetAQAnalytics` had `avg_approval_time_min`/`sla_compliance` hardcoded to fixed literals (12.4/91.2) despite the real `approved_at`/`created_at`/`due_at` columns already existing to compute both — wired to real aggregations. `GetAQEvidence` was a fully fabricated "investigation drawer" (fake incident id, 3 fake alerts, a fake threat-intel card, a fake process tree, fake security-event logs) that completely ignored the `:id` param — the same "huge fake-data drawer" pattern already fixed on the Alerts page — rewired to real data derived from the request's own stored `incident_id`/`alert_id`/`target_asset`/`target_user` fields (a real `incidents` row, real related `alerts` sharing the same agent, a real IOC match); `process_tree`/`recent_logs` dropped entirely (no real per-request linkage to either exists anywhere). `GetAQAnalytics`'s `by_category` (5 hardcoded rows) and `by_team` (no real team concept exists — only a free-text `current_approver` username) replaced with real `by_category`/`by_approver` aggregations; `trend`'s 8 hardcoded stale-dated entries wired to real daily counts. `GetAQApprovers` was a fully fabricated 6-person fake org chart — replaced with the real `users` table. `PostAQReport` was a fully fabricated static report (132 requests, 94.7% approved, fictional recommendations) — rewritten to real statistics plus an LLM summary grounded only in those numbers. The Overview tab's "Approval Flow" card computed 4 of its 5 rows as arbitrary fixed-percentage splits (`Math.floor(dash.approved * 0.25)`, etc.) of the one real number it had — replaced with a real `by_policy` breakdown from the backend; its "By Request Type" card was a fully static 5-row fake percentage list disconnected from `dash` entirely — replaced with a real `by_category` breakdown. Found a real, separate infra gap: the `expired` status/count already existed everywhere (dashboard, analytics, `STATUS_COLOR` map) but nothing ever transitioned a stale pending request into it — added `ExpireStaleApprovals()` to the existing 30s scheduler loop in `services/scheduler_service.go`, confirmed live against the 2 real pending requests already sitting in this dev environment from earlier OT/ICS testing. Also added missing `try/catch` error handling to `onDecide`/`onDelegate`/`onEmergency`/`onAddComment` (the `aqAPI` client itself never had the swallow-errors anti-pattern found on other pages — just a missing catch at the call site). **Like every other page this phase**: this page's tables had only 2 real (non-seeded) rows from live-testing — added `seedApprovalQueue` to `cmd/seed/demo/main.go` (6 policies, 6 requests spanning pending/approved/rejected/expired with real decisions/audit-trail/comments), guarded on a seeder-specific `AQ-SEED-%` prefix rather than a blanket count so it coexists with the pre-existing organic rows; caught and fixed a real duplicate-seed bug in the seeder's own first draft where the policies block shared the requests block's idempotency guard, so a partial manual delete (done once during verification) caused a re-run to silently duplicate all 6 policies — gave policies their own independent existence check.

41. **Vulnerabilities** (`/vulnerabilities`) — `vulnerabilities.spec.ts`. A fabrication footprint on par with the largest pages this phase, spread across nearly every tab, on top of a real, previously-latent parameter-index bug. `PostVMFindingAction`'s "accept_risk" case had `WHERE id=$2 AND tenant_id=$3` with only 2 args bound (`fid, tid`) — `$3` was never bound, so the UPDATE always errored and the error was silently discarded — "Accept Risk" had never actually updated anything despite always returning `{"ok": true}`. Fixed the parameter indices and added real error/not-found handling to all three actions. `GetVMDashboard`'s `risk_score`/`mttr_days`/`patch_sla_compliance`/`assets_affected`/`overdue` were hardcoded despite real `risk_score`/`detected_at`/`patched_at`/`asset_id` columns already existing to compute all five — wired to real aggregations with a shared per-severity SLA-window definition (critical=7d/high=30d/medium=90d/low=180d) reused across the dashboard, analytics, compliance, and report endpoints. `GetVMCompliance` was a fully fabricated 4-framework grid (identical for every tenant, fictional "finding" text) — replaced with the shared real `fce_frameworks`/`fce_controls` tables (the established fix shape for this pattern, already used by Container/Supply Chain/OT-ICS). Wiring this in surfaced two further real, previously-latent bugs: the per-framework controls query joined on `fce_frameworks.id` (an integer) when `fce_controls.framework_id` is actually the framework's TEXT code — every framework showed 0 controls despite hundreds of real rows; and even after fixing the join, `fce_controls.description` is nullable with no `DEFAULT`, so scanning `NULL` into a plain Go string silently failed every real control row's scan (the same NULL-scan bug class found on Approval Queue, this time on a shared table this handler doesn't own) — fixed with `COALESCE` rather than altering a table other pages depend on. `GetVMAttackSurface` was 100% hardcoded (fake exposed services, fake TLS certs with dates computed fresh from `time.Now()` every request, fake DNS records, fake firewall rules) — internet-exposed-asset counts and exposed services now derive from real `vm_assets.internet_facing`/`open_ports`, firewall exposure from a real `firewall_rules` query; certificates/DNS-exposure dropped entirely (no certificate store or DNS zone tracking exists anywhere in this codebase) — removed the now-dead "TLS Certificates" card from the frontend. `GetVMThreatIntel`: the frontend's own section title ("CISA KEV Catalog — Matched Findings") already promised per-tenant matching, but the handler returned 3 fixed CVEs regardless of tenant — now genuinely derived from this tenant's own `kev_listed`/`actively_exploited`/`exploit_maturity`/`threat_actors` findings; dropped `campaign`/`first_observed`/`public_poc`/`metasploit`/`exploit_db`/`country`/`motivation`/`target_sectors` fields with zero real backing anywhere. `GetVMAnalytics`'s `mttr_days`/`patch_sla` were hardcoded duplicates of the dashboard's fake values; `top_vulnerable_assets`/`top_cves`/`risk_trend`/`patch_sla_breakdown` were all hardcoded — wired to real aggregations. `PostVMReport` was a fully fabricated static report — rewritten to real `key_metrics`/`top_risks` plus an LLM summary grounded only in those numbers, matching the pattern from Playbooks/Approval Queue. **Like every other page this phase**: none of this page's 5 own tables had any seed data anywhere — added `seedVulnerabilityManagement` to `cmd/seed/demo/main.go` (4 assets, 8 findings spanning every severity/KEV/exploit-maturity combination with real timestamps to exercise MTTR/SLA math, 3 scans, 1 exception, 3 patches), plus 2 real "wide open" `firewall_rules` fixtures since every pre-existing `0.0.0.0/0` rule this tenant had was a real drop/deny rule, not an exposure.

42. **Vuln Queue** (`/vuln-queue`) — `vuln-queue.spec.ts`. The remediation-workflow layer (assignment/status/verification/blockers/exceptions) sitting on top of `vm_*` raw scan findings, backed by its own `vq_*` tables — better-built than most pages this phase (every nullable column already scanned into a Go pointer type, so this page never hit the NULL-scan bug class found on Approval Queue/Vulnerabilities, and the queue/item/exceptions/dependencies handlers already had real error checks throughout). `PostVQAI` called the real LLM but discarded its response almost entirely — the fields the frontend actually renders (`recommendation`/`steps`/`estimated_effort`/`risks_if_delayed`/`alternative_mitigations`) were always the same fixed literal content regardless of whether the LLM call succeeded, with the real response only exposed via a barely-rendered separate `ai_analysis` field — rewritten so a successful LLM response becomes the primary content, matching every other page's AI endpoint. `GetVQDashboard`'s `team_breakdown` was 5 hardcoded fake teams and `mttr_days` a hardcoded literal despite real columns existing for both — wired to real aggregations. `GetVQAnalytics`'s `sla_compliance`/`overdue_count` were hardcoded despite the identical real query already used one function away in `GetVQDashboard`; `team_performance`/`remediation_trend`/`top_delayed_assets` were hardcoded fake arrays; `sla_by_priority` was hardcoded and also completely unrendered anywhere in the frontend — dropped rather than fixed. `GetVQSLA`'s `current_compliance` was 4 hardcoded percentages suspiciously identical to `GetVQAnalytics`'s old fake `sla_by_priority` (the same fabricated numbers copy-pasted between two endpoints) — replaced with a real per-priority on-time percentage from `vq_items`; `policies` (SLA-hour thresholds) kept as legitimate static config. `PostVQReport` was a fully fabricated static report including a specific fake historical comparison ("down from 12.1 days last month") — rewritten to real `key_metrics`/`overdue_items` plus an LLM summary grounded only in real numbers. Frontend: `doAssign`/`doAction`/`doVerify`/`doAddBlocker`/`doBulk` had zero error handling despite the `vqAPI` client already correctly not swallowing errors — added real try/catch to all five plus the exception create/approve/delete handlers; also found the "New Exception" form never refreshed the exceptions list after a successful create (stayed invisible until a full page reload) — added a real refresh. Found and fixed a real React key-collision bug while running the new spec against real seeded data for the first time: "Top Delayed Assets" used `key={a.asset}`, harmless with the old fake data's 5 distinct fixed names, but the real seeded data has two different overdue CVEs on the same real asset — a normal real-world scenario, not a seeding mistake — producing a genuine duplicate-key console error; fixed by keying on `${asset}-${index}`. **Like every other page this phase**: none of this page's 3 own tables had any seed data anywhere — added `seedVulnQueue` to `cmd/seed/demo/main.go` (8 items spanning every status/priority/team combination with real timestamps to exercise MTTR/SLA/overdue math, 1 blocked item with a real dependency row, 1 exception).

43. **Suppression** (`/suppression`) — `suppression.spec.ts`. The alert-noise-reduction rule engine backed by `sup_*` tables — a separate system from the older `suppression_rules` table used elsewhere (`api/suppression_geoip_health.go`'s `GetSuppressionRules`/`CreateSuppressionRule`, confirmed via grep this page's frontend never calls it; left that file untouched). Noticeably more careful backend than most pages this phase — `GetSupDashboard`/`GetSupAnalytics` already had explicit "no per-day suppression-event table exists, so return empty rather than fabricate" comments — so the real bugs found here were more subtle. `PostSupAI` called the real LLM and then discarded its response almost entirely, the same bug shape just fixed on Vuln Queue's `PostVQAI` found here as a second independent occurrence — the fields the frontend actually renders were deterministic Go logic regardless of whether the LLM call succeeded, with the real response only reachable via a completely unrendered `ai_analysis` field — rewritten so a successful LLM response becomes the primary content. `PostSupApprove` always wrote `rule_id=0, rule_name=''` to the audit trail regardless of which real rule was approved/rejected — confirmed live every approve/reject audit entry rendered with a blank "Rule" cell; `PatchSupRule`/`DeleteSupRule` had the identical `rule_id=0` gap (though only `rule_name`, not `rule_id`, is ever rendered) — fixed all three to look up and pass the real rule id/name. Frontend: `setStatus`/`deleteRule`/`approve`/`save` had zero error handling despite `supAPI`'s write methods already correctly not swallowing errors — added real try/catch to all four. **Like every other page this phase**: none of this page's 2 own tables had any seed data anywhere — added `seedSuppressionEnterprise` to `cmd/seed/demo/main.go` (6 rules spanning active/draft and every priority, with real `total_suppressed`/`last_triggered_at` on the active ones so the dashboard's "top suppressed" list has real data, 2 pending-approval critical-priority rules to exercise the approval workflow, plus a matching audit trail); `PostSupPreview` needed no extra seed data since it queries the real `alerts`/`agents` tables directly, which already have thousands of real rows from every earlier page's seeding this phase.
44. **Quarantine** (`/quarantine`) — `quarantine.spec.ts`. The isolation/containment queue backed by `qe_*` tables (`api/quarantine_enterprise.go`) — a separate system from the older `quarantine.go`/`quarantine_actions.go` (`quarantined_files` table, `/api/quarantine/*` routes), confirmed via grep this page's frontend only ever calls `qeAPI`; left that older file untouched. Most severe finding: `GetQEItem` scanned `owner`/`source_detection`/`incident_id`/`case_id`/`quarantine_reason`/`detection_rule`/`business_impact`/`analyst_notes`/`approved_by`/`release_type` — all nullable with no `DEFAULT` — into plain Go `string`s, and `PostQEItem`'s INSERT never set `analyst_notes`/`approved_by`/`release_type` at all, so every item had NULL there until a later action set one. This failed `Scan()` unconditionally, meaning the item-detail view 404'd for literally every quarantine item ever created — verified live: created a real item via curl, immediately GET'd it, got `{"error":"not found"}`; confirmed via psql all three columns were NULL. Fixed via `COALESCE(col,'')`. Also found `PostQEItem` accepted `expires_at` from the body but never included it in the INSERT column list at all — silently dropped regardless of what was sent — fixed by parsing and persisting it. `PostQEAction`'s "release" case never set `release_type` (fixed to `'manual'`). `PostQEAI` didn't request strict JSON and never stripped markdown fences before the frontend's own `JSON.parse()` — a genuinely successful LLM response wrapped in ` ```json ` fences would silently degrade to showing only `threat_summary`, losing 5 other real fields the UI renders — fixed with a tighter prompt and the same fence-stripping every other page's AI endpoint uses. `PostQEReport` was missing `key_metrics` and `by_type_summary` entirely (not just fabricated — the frontend's `ReportsTab` already rendered both via `Object.entries(...).map(...)`, so those sections were always blank) — rewritten with real computed metrics plus an LLM-grounded summary. Frontend: `doAction`/`doCollectEvidence`/`approve`/`decide` had zero error handling despite `qeAPI`'s write methods already correctly not swallowing errors — added try/catch to all four. **Architectural gap found via exhaustive grep**: `PostQEItem` (create) had zero real callers anywhere in the app — no create-form UI existed on this page, and no other page's response-action dispatcher called it either (unlike Approval Queue's `PostAQRequest`, which has a real external OT/ICS trigger). Confirmed via `AskUserQuestion`: built a real "Quarantine Asset" creation modal on the Queue tab (asset name/type/severity/risk/owner/scope/reason/detection/incident/case/business-impact/expiry) calling the already-correct `PostQEItem` endpoint. **Like every other page this phase**: `qe_items`/`qe_evidence`/`qe_audit` had no seed data anywhere in a fresh dev environment — added `seedQuarantineEnterprise` to `cmd/seed/demo/main.go` (8 items spanning every asset type/severity/status/approval state, with real audit trail and evidence rows on items with `evidence_collected=true`); a stray manually-created test row was cleaned up before seeding, matching the Approval Queue lesson about not letting pre-existing rows interfere with the idempotency guard.
45. **Script Runner** (`/script-runner`) — `script-runner.spec.ts`. Backed by `sr_*` tables (`api/script_runner_enterprise.go`) — a separate system from the older `api/script_runner.go` (`/api/scripts/*` routes), confirmed via grep this page's frontend only ever calls `srAPI`; left the older file untouched. Unusually for this phase, the core execute flow was already real (dispatches a genuine `execute_script` `agent_tasks` row and reads real results back), which is exactly what let several more subtle bugs hide underneath it. Biggest architectural gap, confirmed via `AskUserQuestion`: the Schedule tab's automation was entirely inert — `next_run` was computed once at creation and never recalculated, no ticker ever checked for due schedules, `last_run` stayed NULL forever. Built real dispatch into the existing 30s scheduler tick (`services.RunDueScriptSchedules`), reusing the exact dispatch path `PostSRExecute` already used for a manual run — moved into a new exported `services.DispatchScriptExecution` so the scheduler (which can't import `api` without a cycle) calls the same code instead of a divergent copy; `once` disables itself after firing, `hourly`/`daily`/`weekly`/`monthly` advance on fixed intervals, `cron` reuses this file's existing minimal `NextRunTime` parser (documented limitation, no cron library in this repo), `event`-type schedules are left with `next_run=NULL` since they have no time basis and are never pulled onto the ticker. Live-verifying that fix surfaced a real, separate, pre-existing bug: `next_run`/`last_run` are plain `TIMESTAMP` (no time zone) columns, and `time.Now()` on this dev box's local OS timezone (IST, UTC+5:30) writes local wall-clock digits verbatim, which then compare as if they *were* UTC against Postgres's own `NOW()` — a schedule seeded 2 minutes in the past read back ~5.5 hours in the future and never fired. Fixed by using `time.Now().UTC()` for every `next_run` write this page's code makes, including the shared `NextRunTime` parser (a bonus fix — it also backs the older, unrelated `scheduled_tasks` system's own due-schedule query, which likely has the same latent skew, not otherwise touched this pass). Confirmed live end-to-end: a schedule seeded already-due against a real agent hostname was picked up by the real 30s tick, dispatched a real `agent_tasks` row, and correctly advanced `next_run`. Second major finding: `PatchSRScript` only ever persisted `content`/`status`/`version` — yet the Code Editor's Save button always sends the full script object (`name`/`description`/`language`/`category`/`tags`/`requires_approval`) on every edit, so editing any of those fields on an existing script was silently discarded; confirmed live via a real PATCH-then-GET round trip before and after the fix. Third: `syncExecutionFromAgentTask` never set `execution_time` on completion (only ever left it NULL), so `avg_execution_time`/`automation_time_saved_hours` stayed 0 forever regardless of real completed executions — fixed by computing it from `started_at`/`completed_at` in the same `UPDATE`. That fix then surfaced a fourth, independent bug: `GetSRDashboard`/`GetSRAnalytics` scanned `AVG(execution_time)` — a Postgres `numeric`, not `integer` — into a plain Go `int`, which fails `Scan()` silently (error return discarded), so the average stayed stuck at 0 for a second, unrelated reason even after execution_time started being recorded — fixed by scanning into `float64`, matching the same idiom already used elsewhere in this codebase (e.g. AI Assistant's `avgLatency`). And a fifth, found only via the spec's own execution_time regression test: `GetSRExecution`'s post-sync refresh query re-read `status`/`exit_code`/`stdout`/`completed_at` but not `execution_time`, so the very first read right after a real completion still reported `execution_time: null` even though the row had just been correctly updated — only a second request would ever see it; `GetSRExecutions` (the list) had the identical gap, syncing pending rows but never patching their already-built stale response entries. Fixed both. Smaller fixes: `PostSRAI` didn't strip markdown fences before the frontend's own `JSON.parse()` (frontend already had a raw-text fallback here, so less severe than Quarantine's version, but still a real robustness gap); `PostSRReport`'s `recommendations` were 4 fixed strings regardless of `report_type` or any real data — rewritten to an LLM-grounded summary/recommendations based on this tenant's real execution/script/approval/schedule counts. Frontend: `del` (script library), `save`/`execute` (editor), `create`/`toggle`/`del` (schedules), `decide` (approvals) — 7 call sites total — had zero error handling despite `srAPI`'s write methods already correctly not swallowing errors. **Like every other page this phase**: `sr_scripts`/`sr_executions`/`sr_schedules`/`sr_approvals`/`sr_audit` had no seed data anywhere in a fresh dev environment — added `seedScriptRunnerEnterprise` to `cmd/seed/demo/main.go` (6 scripts spanning every language/category/approval/signed combination, 5 historical executions spanning success/failed/running, 3 schedules — one deliberately already-due against a real agent hostname with real script content so the live scheduler tick has something genuine to dispatch on a fresh dev boot — and 2 approvals, one pending one decided).
46. **Scheduled Tasks** (`/scheduled-tasks`) — `scheduled-tasks.spec.ts`. Backed by `ste_*` tables (`api/scheduled_tasks_enterprise.go`) — a third, separate scheduling system alongside Script Runner's `sr_schedules` and the generic `/api/scheduler/tasks` (`scheduled_tasks` table, used elsewhere, not by this page). This page's entire premise is scheduled automation ("Upcoming Executions" tab, cron builder, next_run countdowns), but confirmed via `AskUserQuestion` (same shape of decision as Script Runner's Schedule tab, one page earlier): there was no ticker anywhere that read `ste_tasks` and dispatched due ones — `next_run_at` was set once at creation and never advanced, tasks only ever ran via the manual "Run Now" button. Built real dispatch into the existing 30s scheduler tick (`services.RunDueSTETasks`), reusing this file's already-real dispatch code (moved to `services` as `DispatchSTETask`/`ExecuteSTETaskNow`, same cross-package reason as Script Runner). Building that surfaced that `dispatchSTETask` blindly sent *any* of the ~26 `ste_tasks.task_type` picklist values straight through as a real `agent_tasks.task_type` — but the real agent executor only understands 21 specific task types, and cross-referencing found only `vulnerability_scan` among the types actually exercised has a genuine 1:1 match; the rest (script-language names with no content field anywhere to send, report generation, webhooks, cleanup jobs) have no agent-side implementation at all. Added an allow-list — anything not in it now fails honestly (`"task_type %q has no execution backend in this build"`) instead of silently sending the agent garbage. Second major finding: `PostSTETask` only ever computed an initial `next_run_at` when `cron_expr` was non-empty — every other `schedule_type` (`one_time`/`hourly`/`daily`/`weekly`/`monthly`/`yearly`, the majority the create form offers) got `next_run_at=NULL` at creation, so those tasks never appeared in "Upcoming Executions" and wouldn't have been dispatchable either — fixed with a shared `NextSTERunTime` helper covering every type (`event_based`/`maintenance_window` correctly stay `NULL`, no time basis). Third: `syncSTEExecutions` existed, was well-written, and had zero callers anywhere in the codebase — every dispatched execution stayed `"running"` forever regardless of real agent completion, and `ste_tasks.success_count`/`failure_count` never updated either — wired into `GetSTEExecutions` (sync on read, patching stale response entries) and a new scheduler-tick `SyncAllRunningSTEExecutions` (background sweep across all tenants). Fourth: `PostSTEApprovalDecide` only ever flipped `ste_approvals.status` — approving a task never actually dispatched it, and `PostSTERunTask` unconditionally re-checks `requires_approval` on every "Run Now" click regardless of any existing decision, so a `requires_approval` task could never run at all (approving did nothing; running again just filed another pending approval, forever) — fixed to dispatch for real on approval. Fifth: `GetSTEAnalytics`'s `by_category` query selected `category` directly from `ste_executions`, which has no such column (only `ste_tasks` does) — an undefined-column error silently discarded via the ignored `Query` return, so `by_category` was always empty — fixed by joining `ste_tasks`, mirroring the `by_task_type` query one line below it which already did this correctly. `PostSTEReport` was the single most minimal fake response found this entire phase — zero real numbers, not even a fabricated snapshot (`"%s report generated successfully. Download will begin shortly."`), no `key_metrics` field for the frontend to render even if it wanted to — rewritten with real computed metrics plus an LLM-grounded summary; the frontend's `ReportsTab` discarded the response entirely (just a toast) — now renders the real title/executive_summary/key_metrics/recommendations panel matching every other page's report view. Frontend: `toggle`/`del` (Task Library), `markNotificationsRead` had zero error handling. **Unlike every other page this phase**, this one already had a seeder (`seedScheduledTasksEnterprise`, 18 realistic tasks/executions/approvals/notifications/audit) — but it had no idempotency guard, and its `ON CONFLICT DO NOTHING` clauses had no actual unique constraint to conflict on, so every fresh `go run ./cmd/seed/demo` silently appended another full duplicate batch. Confirmed live: this dev DB had accumulated 720 `ste_tasks` rows (18 × 40 reseeds) before a standard tenant-count guard was added and the duplicates cleaned up back to the intended 18.
47. **Firewall** (`/firewall`) — `firewall.spec.ts`. Deliberately blends the mature base rules engine (`firewallAPI` → `/api/firewall/*`, backed by `firewall_rules`) with a newer "enterprise" layer (`fweAPI` → `/api/fwe/*`, backed by `fwe_*` tables) for policies/zones/NAT/threats/connections/blocked/approvals/notifications/analytics/audit/AI/reports — confirmed via grep this is an intentional blend, not a wrong-API-used bug. Three tabs (Threat Protection, Live Connections, Blocked List) had zero real writer anywhere in the codebase for their backing `fwe_*` tables. Confirmed via `AskUserQuestion`: bridged what has a real source elsewhere, left what doesn't as clearly-seeded reference data. Live Connections (`fwe_connections`) is now backed by `endpoint_connections` — the real periodic connection-snapshot table agents report via `POST /api/agents/connections` (`repositories.SaveConnections`) — giving real `src_ip`/`dst_ip`/`protocol`/`state`/process data; `bytes_sent`/`bytes_recv`/`duration`/`zone_src`/`zone_dst`/`rule_id` had no real source anywhere in this codebase (no live byte-counting or zone-correlation exists) and were dropped from both the API response and the frontend table's columns rather than fabricated — also caught `GetFWEDashboard`'s `active_connections` and `GetFWEAnalytics`'s `by_protocol` silently reading the same dead `fwe_connections` table, repointed both to the real source. Blocked List (`fwe_blocked`) already had one real write path (manual blocks via `PostFWEBlock`) — but `ioc_firewall_blocks`, a second, genuinely real block source (populated by `services.autoBlockIOC` on real IOC matches), was never surfaced here; `GetFWEBlocked` now merges both, verified live with a real IOC-block row appearing alongside the seeded manual ones. Threat Protection (`fwe_threats`) has no real detection pipeline anywhere in this codebase writing anything in that shape — left as clearly-seeded reference data per the explicit decision, no code change. `total_bytes` (summed from the now-unreferenced `fwe_connections`) had no real source once bridged — dropped from the dashboard response and the frontend's "Total Traffic" `MetricCard` (and the then-dead `bytes()` formatter) rather than left silently reporting a stale number forever. `PostFWEReport` was a fully fabricated one-line stub (`"%s report generated successfully"`, zero real numbers, no `key_metrics`) — rewritten with real computed metrics plus an LLM-grounded summary; the frontend's `ReportsTab` discarded the response entirely (just a toast) — now renders it. Frontend: `deletePolicy`, `firewallAPI.delete`/`.update` (Rules tab), `markNotificationsRead` had zero error handling. **This page's own pre-existing seeder** (`seedFirewallEnterprise`) had the exact same non-idempotency bug just found on Scheduled Tasks one page earlier — its own `CREATE TABLE` statements assume a different schema (`policy_id UNIQUE`, `zone_id`, `interface_names`, etc.) than what the real handler's `createFWETables()` actually creates (which wins, since it runs first in this environment), so every `ON CONFLICT DO NOTHING` had no real unique constraint to ever conflict on. Confirmed live: hundreds of duplicate rows had accumulated across every `fwe_*` table (252 policies, 294 zones, 336 NAT, 504 threats, 420 blocks, etc. — all intended to be single-digit-to-low-teens counts) before a standard tenant-count guard was added and duplicates cleaned up back to the intended 6/7/8/12/6/9/10.
48. **Reports** (`/compliance`) — `compliance.spec.ts`. Backed by `rpe_*` tables (`api/reports_enterprise.go`); also uses a separate, unrelated, genuinely real CSV/JSON export feature (`exportAPI` → `/api/export/*`) for the Dashboard's "Quick Exports" section, confirmed real and left untouched. `PostRPEGenerate` was explicitly commented `// Simulate execution` — `duration_ms`/`file_size_bytes` were pure random numbers, no real report content was ever produced, and the `download_url` it returned pointed at `/api/rpe/download/:id`, a route that was never registered anywhere — every "Download" link this page ever showed (Dashboard quick-actions, History tab) 404'd. Confirmed via `AskUserQuestion` (same shape of decision as the last 3 pages): rewrote report generation to produce real content — real cross-cutting metrics (alerts/incidents/vulnerabilities/compliance, the same real data `PostRPEAI` already gathered for its prompts) plus an LLM-grounded `executive_summary`/`recommendations` — stored in a new `rpe_exports.content` column and served for real by a new `GetRPEDownload` handler, now actually registered at `GET /api/rpe/download/:execution_id`. No PDF/DOCX/XLSX rendering engine exists anywhere in this codebase — rather than fabricate a binary format that can't actually be produced, `json`/`csv` get their real native shape and everything else (`pdf`/`html`/`docx`/`xlsx`, all real `export_format` choices in the UI) gets the same real content as a readable plain-text report instead of a falsely-labeled binary file. `rpe_schedules` had the same dead-scheduler gap as the last 3 pages — `next_run_at` was hardcoded to "now + 24h" regardless of the selected frequency (hourly/daily/weekly/monthly/quarterly/yearly/cron) and nothing ever dispatched due report schedules — fixed with a shared `NextRPERunTime` helper (real per-frequency computation, UTC-correct) and a new `services.RunDueRPESchedules` wired into the existing 30s scheduler tick, dispatching through the same real generation path a manual "Generate" click uses; verified live end-to-end (a schedule seeded already-due was picked up by the real tick, generated a real report, and correctly advanced `next_run_at` by its own real weekly interval). Frontend: `deleteReport`, `deleteTemplate`, `updateSchedule`/`deleteSchedule` (Scheduled tab toggle/delete), `markNotificationsRead` had zero error handling. **This page's own pre-existing seeder** had the same non-idempotency bug found on the last two pages, but only for the 4 tables with no real unique constraint (`rpe_executions`/`rpe_exports`/`rpe_notifications`/`rpe_audit` — each row is a historical event, not a config row); `rpe_reports`/`rpe_templates`/`rpe_schedules` already had real `UNIQUE` constraints and were genuinely idempotent already. Confirmed live: 661/353/353/573 rows had accumulated across those four tables (vs. the intended 15/8/8/13) before a standard tenant-count guard was added.
49. **Frameworks** (`/framework-compliance`) — `framework-compliance.spec.ts`. Backed by `fce_*` tables (`api/framework_compliance_enterprise.go`) — the same `fce_frameworks`/`fce_controls` pair Vulnerabilities' `GetVMCompliance` reads too (a shared, not page-owned, table pair). `PostFCEAI`'s "Top failing controls" query joined `f.id=c.framework_id` — `fce_frameworks.id` is an integer, `fce_controls.framework_id` is the TEXT business key (e.g. `"FWK-000123"`) — comparing integer to text is flatly rejected by Postgres (`operator does not exist: integer = text`). The discarded `Query()` error meant this section of the AI context was silently empty on every call regardless of how many real failing controls existed — the exact same bug class already fixed on Vulnerabilities' `GetVMCompliance` against the same two tables earlier this phase, found here independently. Fixed by joining on the real text `framework_id` (plus `tenant_id`, to keep the join tenant-scoped) — verified live via a direct psql comparison that the old join errors outright and the fixed one returns real matched rows. Frontend: every single write call site on this page had zero error handling — `createFramework`/`updateFramework`/`deleteFramework`, `updateControl` (×2: note save and status dropdown), `addEvidence`/`deleteEvidence`, `createRemediation`/`updateRemediation`, `runAssessment`, `markNotificationsRead` — 12 call sites total, the most error-handling-gap-heavy page found this entire phase. **This page's own pre-existing seeder** had the same non-idempotency bug found on the last three pages, split across two causes this time: `fce_controls` has no unique constraint at all (its own `ON CONFLICT DO NOTHING` with no target never matched anything), and `fce_notifications`/`fce_audit` have no `ON CONFLICT` clause whatsoever (each row is a historical event, not a config object) — while `fce_frameworks`/`fce_evidence`/`fce_assessments`/`fce_remediations` all had real unique constraints their own inserts correctly targeted and were already genuinely idempotent. Confirmed live: `fce_controls` had accumulated 2208 rows (vs. ~48 intended) and `fce_notifications`/`fce_audit` had 460/690 (vs. ~10/~15) before a standard tenant-count guard was added.
50. **Executive** (`/executive`) — `executive.spec.ts`. Backed by `exe_*` tables (`api/executive_enterprise.go`); a separate, unrelated, dead API client pair (`executiveAPI.getMetrics`/`.downloadReport`, hitting the older singular `/api/executive/*` routes) exists in `lib/api.ts` but is never called anywhere on this page — confirmed via grep, left untouched. Overall a well-built, real-data-driven backend (no fake pipeline, no dead scheduler) — the findings here were narrower. `GetEXEVulns` hardcoded `"exploitable"` to `0` always, even though `vulnerabilities.is_kev` (CISA Known Exploited Vulnerabilities) is a real, already-populated column that's exactly what that label means — fixed to count real `is_kev=true` rows (300 real KEV vulnerabilities existed for this tenant already, just never counted). `GetEXEBusinessImpact` returned `max_potential_loss`/`avg_recovery_cost`/`cyber_insurance_coverage` hardcoded to `0`/`0`/`""` always — no actuarial/incident-cost model or insurance-policy data exists anywhere in this codebase to compute them honestly, and the frontend rendered all three as real `MetricCard`s ("$0", "$0", "—") indistinguishable from a genuinely-computed zero — dropped from both the API response and the frontend, matching the "drop what can't be honestly backed" precedent from Vulnerabilities' TLS-certificate removal and Firewall's `total_bytes` removal. `PostEXEReport` inserted a metadata-only row with a fake randomish `size_bytes` (`420000+time.Now().Unix()%200000`) and left the table's own `summary`/`key_findings`/`recommendations` columns permanently NULL/empty — a narrower, less severe version of Reports page's `// Simulate execution` bug (no dead download link here, this page never promised one — just unused schema columns and a fake number). Now generates a real LLM-grounded summary/findings/recommendations from the same real security/risk metrics `PostEXEAI` already gathers, with `size_bytes` reflecting the real generated content's length (119 bytes for a real report, vs. the old fake ~420,000-620,000 range) — added a "View" action + modal to the frontend's Reports tab so the real content is actually visible somewhere, since nothing rendered it before. Frontend: the report-generation button silently swallowed errors via `.catch(() => null)` with zero user feedback — fixed to surface real errors. **This page's own pre-existing seeder** had the same non-idempotency bug found on the last four pages: `exe_snapshots`/`exe_forecasts`/`exe_reports`/`exe_integrations` all had real unique constraints and were already genuinely idempotent, but `exe_notifications`/`exe_audit` had none. Confirmed live: 480/576 rows had accumulated (vs. ~10/~12 intended) before a standard tenant-count guard was added.

---

## ⬜ Remaining (4), in sidebar order
- [ ] SOC Metrics (`/soc-metrics`)
- [ ] Assets / CMDB (`/assets`)
- [ ] Mobile / MDM (`/mdm`)
- [ ] AI Assistant (`/ai-assistant`)
- [ ] Settings (`/settings`) — start from the session-revocation-enforcement gap flagged during UEBA/Insider Threat
- [ ] Tenants (`/platform`, platform-admin only)
