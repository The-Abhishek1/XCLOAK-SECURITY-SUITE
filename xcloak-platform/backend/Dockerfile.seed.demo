FROM golang:1.25-alpine AS builder
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -trimpath -ldflags="-s -w" -o /out/demo-seeder ./cmd/seed/demo

FROM alpine:3.19
COPY --from=builder /out/demo-seeder /demo-seeder
ENTRYPOINT ["/demo-seeder"]
